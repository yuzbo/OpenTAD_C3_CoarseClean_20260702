---
doc_id: BUILDER_DUCA_P0_DENSITY_MINIMAL_CHANGE_PLAN
version: v001
stage: DRAFT
author_role: coordinator-dispatch
parent_decision: PRO_P0_ROUTE_ADJUDICATION-v002
github_revision: 63a726a4aaf48ecbf6780bb196de43a890c6b4df
evidence_class: BLOCKED_PRE_RESULT
---

# Builder launch card — P0 density route minimal change plan

Work only from the clean Builder worktree at the frozen revision. Before any
editing or test command, return a `MINIMAL_CHANGE_PLAN` for the accepted
`DUCA_FIXEDK_BOUNDED_DENSITY_QUANTILE_ACQUISITION-v002` decision.

The plan must identify the smallest existing-code path for three separable
units: (1) the one canonical endpoint-inclusive integer-half-up uniform
generator and its exact call sites; (2) exactly-once selected-q to
physical-dense transport before all per-sample NMS; and (3) a density-only
reader/projection producing `duca_density_logits` from dense `browser_memory`,
with `selection_unit=1`, the identity physical grid, a hard decoder before
legacy selector dispatch, and hard gathering only.

State the exact symbols/paths, data shapes and coordinate states, no more than
ten likely production/test/config files, the smallest authored-but-not-run
fixtures, and every point where the plan must stop rather than change the
detector/head/loss/NMS/evaluator/split/class map or reinterpret a legacy signal
as density. Explicitly prove the plan needs no new framework, scheduler,
wrapper, source hash tree, or SHA manifest.

This launch card authorizes neither code edits nor any command execution. Do
not access data, run Python/CPU/GPU tests, create Slurm jobs, alter Git state,
push, calculate metrics, or inspect prior quarantined patches. End with a
no-execution attestation and return only the durable plan path.
