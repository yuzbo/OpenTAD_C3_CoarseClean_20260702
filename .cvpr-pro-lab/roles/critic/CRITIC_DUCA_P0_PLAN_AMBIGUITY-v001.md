---
doc_id: CRITIC_DUCA_P0_PLAN_AMBIGUITY
version: v001
stage: DRAFT_P0_PLAN_REVIEW
author_role: coordinator
parent_decision: PRO_P0_BLOCKER_DECISION-v001
parent_builder_return: BUILDER_DUCA_P0_MINIMAL_CHANGE_PLAN-v002
github_revision: 63a726a4aaf48ecbf6780bb196de43a890c6b4df
evidence_class: BLOCKED_PRE_RESULT
---

# Critic request: classify the P0 plan ambiguity

The bound Builder plan identifies the two specified uniform call sites and the
pre-NMS transport insertion point. It also reports that frozen revision
`63a726a4` contains no identifiable density hard-decoder/wrapper on which the
Pro-mandated exact constant-density specialization can be attached.

Perform a read-only source/plan review only. Do not patch, test, run code,
access data, use remote resources, or inspect the quarantined Builder worktree.

Return exactly one primary classification:

1. `IMPLEMENTATION_CORRECTION` only if a named existing symbol and a minimal
   claim-preserving attachment point can be established from the frozen tree;
   give paths/symbols and explain why no scientific contract changes.
2. `SCIENTIFIC_AMBIGUITY` if the accepted P0 decision requires a density
   mechanism not represented by the frozen implementation, or if choosing one
   would define model behavior/route rather than repair an interface. State the
   smallest precise question for a fresh Pro decision.

Also check whether the proposed canonical uniform and pre-NMS transport surface
stays within the accepted detector/config/metric/split invariants. This is not
the full P0 diff closure; it is a plan-viability classification required before
any patch is authorized.

Finish with a no-execution attestation and evidence class only.
