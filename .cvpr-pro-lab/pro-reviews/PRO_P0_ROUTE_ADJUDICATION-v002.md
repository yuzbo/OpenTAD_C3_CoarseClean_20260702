---
doc_id: PRO_P0_ROUTE_ADJUDICATION
version: v002
status: accepted_local_pending_project_source_sync
date: 2026-08-11
supersedes: PRO_P0_BLOCKER_DECISION-v001.md
stage: DRAFT
author_role: pro-scientific-first-author-agent
github_repo: https://github.com/yuzbo/OpenTAD_C3_CoarseClean_20260702.git
github_revision: 63a726a4aaf48ecbf6780bb196de43a890c6b4df
project_id: g-p-6a796fef9a00819194024cf1de3bd697
dispatch_id: CENTRAL-PRO-DUCA-ORACLE-P0-20260811-v003
oracle_session: duca-p0-oracle-v003
turn_id: duca-p0-oracle-20260811-8b12d6f4a90c4b9a
nonce: 8b12d6f4a90c4b9aa602f1d9b3e75c18
source_transcript: .cvpr-pro-lab/pro-reviews/runs/duca-p0-oracle-formal-v003/raw-response.md
---

# PRO_P0_ROUTE_ADJUDICATION-v002

## Intake

Routing proof passed: the configured Project was used through a fresh Oracle
browser conversation; the new nonce, pinned commit, and required v005 Sources
match the request. Oracle browser evidence recorded the `Pro` picker target as
verified. The response is scientifically sufficient and is accepted without a
format repair request.

## Scientific decision

`REVISE`. Supersede the ambiguous density statement in v001 with the explicit
candidate `DUCA_FIXEDK_BOUNDED_DENSITY_QUANTILE_ACQUISITION-v002`. This is still
offline, fixed-requested-K=384, pre-backbone acquisition; it is not a result,
an efficiency claim, a detector change, or an authorized experiment.

The sole learned decoder input is a new per-sample, valid-prefix tensor
`duca_density_logits[b,t]`, generated by a dedicated density-only reader from
the dense `browser_memory` attachment representation while `selection_unit=1`
keeps the identity physical candidate grid. Existing slot allocation,
frame-selection logits, actionness/boundary heads, soft transport, top-k/rank,
quota, and dynamic-budget policies must not be aliased as the density.

The hard decoder is `decode_duca_density_positions_v001(density_logits_valid,
requested_k=384)`. It uses finite per-time logits, fixed `1e-6 + softplus`,
per-sample trapezoidal interval masses, endpoint-inclusive inverse-CDF
quantiles `j/(K_eff-1)`, and a deterministic constrained integer projection.
`K_eff=min(384,16*floor(T_v/16))`; `T_v<16` fails closed. The projection keeps
count, endpoints, strict ordering, adjacent span at most 4, and displacement at
most 16 from canonical uniform positions. A solver failure, unresolved tie,
non-finite input, or violated constraint fails closed. No gradient surrogate,
detector-gradient path, contribution teacher, or experiment is admitted.

Exactly equal constant valid-prefix logits invoke the one canonical endpoint-
inclusive integer-half-up uniform generator, with no tolerance test. Near-
constant inputs follow the density decoder.

## Deterministic corrections

Two claim-neutral implementation corrections may proceed separately from the
new density wrapper:

1. One shared canonical exact-uniform generator using the frozen integer
   half-up endpoint arithmetic. It corrects the 766/767 endpoint disagreement
   but proves neither learned acquisition nor performance.
2. An exactly-once selected-q to physical-dense segment adapter at the start of
   every per-sample `SingleStageDetector.post_processing`, before filtering,
   top-k, IoU, or NMS. It corrects coordinate semantics only; detector/head,
   losses, NMS configuration, evaluator, split, and class map remain unchanged.

## Falsifiers and gates

- A constant-logit T=768/K=384 fixture that does not equal the canonical vector
  ending at 767, or any density alias to an existing selector signal, blocks the
  route.
- A trace showing NMS receives selected-q rather than physical coordinates, or
  clean/wrapper index mismatch, blocks P0.
- Evidence remains `BLOCKED_PRE_RESULT`. No training, evaluation, data access,
  metric, GPU, Slurm, checkpoint, Git push, result promotion, or paper claim is
  authorized by this decision.

## Bounded role work

1. Builder first returns a current minimal change plan for the density API and
   two deterministic corrections at the frozen commit. Only after that plan is
   reviewed may it produce the bounded patch and authored-but-not-run fixtures.
2. Critic starts after the complete Builder diff and returns only
   `P0_STATIC_PASS` or `P0_BLOCKED`, with a minimal counterexample for blockers.
3. Evaluator prepares a no-execution P0/P1 protocol amendment which freezes the
   decoder properties and future remote-CPU receipt contract. P1 and PRE_RUN
   remain blocked.

Project-local rules supersede the response's proposed SHA-manifest artifacts:
no SHA tree/manifest is required or authorized. The raw Pro response remains
the complete original record; this document is its compact operational intake.
