---
title: PRO_DUCA_UOR_TERMINAL_DISPOSITION
version: v001
date: 2026-08-14
project_id: g-p-6a796fef9a00819194024cf1de3bd697
turn_nonce: DUCA-UOR-TERMINAL-v001-20260814T013510Z
decision: REVISE-AUTHORIZE
evidence_class: BLOCKED_PRE_RESULT
pinned_commit: 6576789468c1a7692d49b2ba94a638e01e7970f4
raw_stdout_stderr: C:/Users/skywalker/.fastctx/jobs/j-aiatnz/output.log
---

# Pro terminal disposition: sealed execution surface replacement

The old U/O/R package at `6576789468c1a7692d49b2ba94a638e01e7970f4` remains
terminally failed and immutable. The prior 21 passing no-data checks remain
implementation-contract history only. The second equivalent reachable
`--cfg-options` launch bypass closes the old one-correction loop; it must not
receive a third correction, further Critic recheck, Evaluator PRE_RUN, or any
execution.

Pro chooses `REVISE-AUTHORIZE`: one genuinely separate, additive replacement
named `DUCA_UOR_SEALED_EXECUTION_SURFACE-v001`. It replaces the generic launch
and configuration surface rather than adopting the unapproved “reject nonempty
`--cfg-options`” patch. The frozen scientific arm semantics, physical-time
density mechanism, FIT/CAL population boundary, official-validation firewall,
decision statistic, and claim boundaries are unchanged.

## Authorized bounded package

Builder may make one clean descendant of the pinned commit with at most six new
production/configuration/launcher files, at most eight focused static or
synthetic no-data test files, and one tracked commit. The old DUCA launcher,
generic gate helper, `tools/train.py`, `tools/test.py`, and their route/stage
protection must not change; needing such an edit is `REPLACEMENT_BLOCKED`.

The replacement must contain a dedicated sealed runner accepting only a
canonical JSON manifest plus the fixed phase set `detector_fit|reader_fit|cal_u|cal_o|cal_r`, a closed unknown-key-rejecting manifest schema without any
gate-disabling field, a dedicated sealed evaluator adapter, and a zero-argument
literal future launcher. It must not call generic train/test entrypoints, merge
generic configuration, accept overrides, or import data/checkpoint/output
surfaces before its unconditional firewall completes.

Builder acceptance evidence is limited to changed-file and commit identity,
static dependency and pre-import traces, accepted/rejected argument and manifest
matrices, synthetic forbidden-root tests, literal future argv/phase order, and
attestations that the old package and untracked prototype were untouched. It is
no-data, no-GPU, no-remote, no-dataset, and no-metric work.

## Single-pass review chain

After the sole Builder submission, independent Critic performs exactly one
terminal review and returns only `SEALED_REPLACEMENT_STATIC_PASS` or
`SEALED_REPLACEMENT_BLOCKED`. It must attack argument and environment injection,
manifest mutation, official-validation substitution, phase-order/output-receipt
bypass, generic entrypoint reachability, arm asymmetry, and evaluator/coordinate
drift. A block is terminal STOP: no Builder response or recheck.

Only a Critic static pass reaches the existing Evaluator for one structural,
synthetic/no-data intake. The Evaluator returns only
`SEALED_REPLACEMENT_STRUCTURAL_PASS / PRE_RUN_NOT_READY` or
`SEALED_REPLACEMENT_BLOCKED`. Neither outcome authorizes data access, FIT/CAL
manifest construction, launcher execution, PRE_RUN admission, GPU/Slurm/remote
work, training, inference, evaluation, metrics, or claims. Any block is STOP.

## Scientific boundary

The potential publishable observation remains narrowly that deploy-visible
density acquisition can recover meaningful diagnostic density headroom over
matched exact-uniform physical acquisition at fixed budget and frozen detector,
without violating high-tIoU protection, on training-population CAL videos.
No such observation exists. The later one-time CAL U/O/R comparison is the
cheapest route-changing falsifier, but is not authorized here. A future pass
would be only training-population mechanism reachability, not official-validation
efficacy, efficiency, generality, paper readiness, or a result-table claim.
