PARALLEL-SAFE-FRESH-PRO-20260811::019fa3db-42bf-7f30-a0ab-2b8171ab33ed::g-p-6a796fef9a00819194024cf1de3bd697::7e2b1f8c4d0a6e53c912b7f4e8d2a901

# DUCA P0 decoder adjudication — fresh scientific decision

You are the acting Scientific First-Author Agent and Primary Research Owner for
this exact DUCA model, experiment programme, and CVPR paper. You alone make the
scientific route decision. Codex is only implementation, adversarial review,
evaluation, and evidence transport; it cannot select or silently alter the
method, claim, baseline, or experiment route.

This is a wholly new conversation in the exact ChatGPT Project below. Do not use
or continue any earlier conversation. Do not use any material from another
project. The coordinator will quarantine the response if its routing identity or
scientific context is not exact.

## Exact routing and code identity — echo verbatim at the beginning

`MARKER=PARALLEL-SAFE-FRESH-PRO-20260811::019fa3db-42bf-7f30-a0ab-2b8171ab33ed::g-p-6a796fef9a00819194024cf1de3bd697::7e2b1f8c4d0a6e53c912b7f4e8d2a901; COORDINATOR=019fa3db-42bf-7f30-a0ab-2b8171ab33ed; PROJECT_ID=g-p-6a796fef9a00819194024cf1de3bd697; NONCE=7e2b1f8c4d0a6e53c912b7f4e8d2a901; GIT_COMMIT=63a726a4aaf48ecbf6780bb196de43a890c6b4df; CURRENT_STATE=CURRENT_RESEARCH_STATE-v005; HISTORY=MODEL_EXPERIMENT_HISTORY-v005`

- Project URL: `https://chatgpt.com/g/g-p-6a796fef9a00819194024cf1de3bd697/project`
- Canonical GitHub: `https://github.com/yuzbo/OpenTAD_C3_CoarseClean_20260702.git`
- Exact review commit: `63a726a4aaf48ecbf6780bb196de43a890c6b4df`
- The dirty coordinator checkout is not code truth. Inspect only the canonical
  GitHub repository at the exact commit.
- The browser chose its highest available Pro route. State the model and effort
  that you actually observe, but do not treat an omitted self-report as a reason
  to evade the decision.

## Project Sources that must be read and reconciled

Read the confirmed Project Sources in full. The core source set is:
`PROJECT_CHARTER-v001`, `LITERATURE_AND_GAP-v001`, `ROUTE_DECISION-v001`,
`EXPERIMENT_PLAN-v001`, `IMPLEMENTATION_STATUS-v001`, `RESULTS-v001`,
`RESULT_ANALYSIS-v001`, `FAILURES_AND_PIVOTS-v001`, `CLAIM_MAP-v001`,
`PAPER_DRAFT-v001`, `CURRENT_RESEARCH_STATE-v001`, and
`MODEL_EXPERIMENT_HISTORY-v001`.

Also reconcile the later confirmed materials:
`PRO_INITIAL_REVIEW-v002`, `PRO_P0_BLOCKER_DECISION-v001`,
`EVALUATOR_DUCA_DENSITY_P0P1-v001`, all later state/history versions, and in
particular the authoritative current pair `CURRENT_RESEARCH_STATE-v005` (the UI
may show `CURRENT_RESEARCH_STATE-v005(1).md`) and
`MODEL_EXPERIMENT_HISTORY-v005`. Where sources conflict, v005 is current for
the P0 blocker; historical runs, subset metrics, failed roots, stress-test
responses, and advisory proposals remain historical only.

## Paper boundary and evidence state

The task is offline Temporal Action Detection on THUMOS14 using the repository's
official OpenTAD protocol. It is not Online TAD or causal streaming. The target
is a pre-heavy-backbone acquisition plugin: deploy-visible low-cost evidence
should choose exact, ordered, unique physical-time heavy-frame positions under a
fixed K, while keeping detector/head/loss/NMS/evaluator semantics unchanged.

The evidence class is **BLOCKED_PRE_RESULT**. No result in this turn may be
promoted to, or numerically compared against, the historical approximately-65
AdaTAD/uniform reference. There are no P0 patches, tests, data traversals,
metrics, CPU/GPU/Slurm jobs, checkpoints, costs, or paper results associated
with the v005 finding. Do not fabricate such evidence.

The accepted prior scientific status is `REVISE`. The candidate is
`DUCA_FIXEDK_BOUNDED_MONOTONE_DENSITY_ACQUISITION-v001`, not an implemented or
selected final method. Two independent static reviews establish:

1. The exact frozen code has no named authoritative positive per-time learned
   density tensor, no inverse-CDF hard decoder, and no constant-density forward
   specialization. Existing slot allocation, top-k, and rank-transport symbols
   have different meanings; reinterpreting one would silently choose a new
   method.
2. Two changes look deterministic rather than scientific: a single
   endpoint-inclusive integer-half-up exact-uniform generator (T=768,K=384 must
   end at 767), and mapping raw per-sample proposals from `selected_q` to
   `physical_dense` before filtering, top-k, IoU, and unchanged NMS. For the
   non-sliding detector path, selected-to-physical conversion after NMS is
   forbidden.

## The two questions you must adjudicate now

1. For the candidate route, name the **authoritative existing input** that must
   become the nonnegative per-time density, and specify the smallest faithful
   hard-decoder API and insertion point. The answer must be implementation-aware:
   give relevant repository paths/symbols, normalization/positivity, exact-K,
   uniqueness, monotonicity, endpoint and short-window semantics, masks, and a
   constant-density specialization. If the frozen implementation cannot support
   this without introducing a new scientific mechanism, explicitly choose
   `REVISE`, `PIVOT`, or `STOP` and forbid Codex from inventing it.
2. Decide whether the integer-half-up uniform control and pre-NMS
   `selected_q -> physical_dense` transport are deterministic corrections that
   Builder may implement and test before the learned-density mechanism is
   settled, or whether they must wait. They may not alter the detector, head,
   loss, NMS algorithm, evaluator, data split, metric, or budget semantics.

Do not broaden this review into dynamic-K, a new detector, post-backbone fusion,
extra loss plugins, a new worktree family, or a new benchmark. Do not prescribe
free numerical thresholds merely because prior discussion mentioned them.

## Required response: PRO_P0_DECODER_ADJUDICATION-v001

Return a coherent versioned decision with these headings:

1. `SESSION_ASSERTION` — state this is a new conversation, echo the exact line
   above, and state that you saw no other-project material.
2. `MODEL_EFFORT_ASSERTION` and `ROLE_ACKNOWLEDGMENT`.
3. `CONTEXT_USED` — exact commit, Project Sources actually used, and any access
   limitation or stale source.
4. `HISTORY_SYNTHESIS` and `PAPER_OBJECTIVE` — concise but accurate distinction
   between current candidate, prior routes, historical diagnostics, and no
   admissible results.
5. `CURRENT_JUDGMENT` — implementation fidelity, novelty, fairness, leakage,
   reproducibility, and overengineering risks.
6. `SCIENTIFIC_DECISION` — exactly one of `CONTINUE`, `REVISE`, `PIVOT`, `STOP`,
   `ESCALATE_HUMAN`, with an unambiguous permitted/forbidden scope.
7. `AUTHORITATIVE_DENSITY_AND_DECODER_SPEC` — exact decision for question 1.
   If continuation is legitimate, include the mathematical contract and minimal
   API-level change. If not, name the missing fact and prohibit an interpretive
   patch.
8. `DETERMINISTIC_CORRECTION_RULING` — exact decision for question 2, including
   an invariant test list and no-double-transport rule.
9. `ROUTE_AND_CLAIMS` — at most two future claims, anti-claims, falsifier, and
   strict evidence boundary.
10. `CODEX_DISPATCH` — three independent, queue-ready briefs for Builder,
    Critic, and Evaluator. Each must name allowed paths/actions, expected durable
    artifact, focused test/audit, stop condition, and whether execution must
    remain static/fixture-only or may move to PRE_RUN. Do not assign experiments
    that do not follow from your decision.
11. `EXPERIMENT_PLAN` — only the smallest ordered ladder needed after P0, with
    the strongest fair official baseline, formal split/evaluator/checkpoint/seed
    principles, cost boundary, and stop rule. Clearly separate preparation from
    formal evidence.
12. `PUBLICATION_PLAN` and the ten-item `DRIFT_CHECKLIST`.
13. `NEXT_RETURN_CONTRACT` — precise versioned Builder/Critic/Evaluator evidence
    that must return before another fresh Pro decision.

You are the scientific owner: do not give Codex a menu of routes and ask it to
choose. Do not invent literature, performance, access, or code facts. Preserve
negative/failure evidence and state what must not be done now.

PARALLEL-SAFE-FRESH-PRO-20260811::019fa3db-42bf-7f30-a0ab-2b8171ab33ed::g-p-6a796fef9a00819194024cf1de3bd697::7e2b1f8c4d0a6e53c912b7f4e8d2a901
