# DUCA frozen contract — second and final Pro reconciliation

Fresh nonce: `DUCA-PHYSICAL-CLIPLET-FINAL-20260820T180224Z-7fac8d4a372c5fe455d54ee6`

This is the second and final scientific review round in the exact DUCA Project. Read the original frozen contract and the complete first-round transcript. The first round returned `REVISE`; its scientific kernel is accepted, but its text is not yet safe to implement. Act again as both a severe CVPR reviewer and a first-author researcher, and issue one final, internally consistent, Builder-ready contract in Chinese. Do not reopen the route or ask for another review.

## Binding human/project constraints

- Headline remains indirect actionness/start/end semantic prediction → deterministic acquisition → per-window dynamic outer budget. Fixed K is control/fallback; direct index learning is not the headline.
- Physical time must exist before proposal filtering/top-k/IoU/NMS; actual VideoMAE compute must be reduced.
- Dense, equidistant-uniform and random trainings must not be duplicated. Existing immutable receipts may only be read and compatibility-audited.
- The next run, after implementation/Critic/Evaluator PRE_RUN, must use complete canonical THUMOS14 and official evaluator. A first seed may gate expansion, but it is a full-data/full-schedule run, never a subset/pilot efficacy claim.
- This is still design adjudication; do not claim implementation, PRE_RUN, metric or execution.

## Deterministic defects in the first-round answer that must be corrected

1. The phrase requiring `atom hash、permutation hash、inverse-permutation hash 全部相等` is mathematically wrong: a forward permutation generally differs from its inverse. The only needed invariants are exact equality of ordered or sorted frame/atom IDs between arms, bijection of the permutation, and exact round-trip identity `inverse(forward(x)) == x`. This repository explicitly forbids unnecessary hashes/checksums. Remove every manifest/atom/permutation hash requirement and use direct tensor/array equality plus bijection/round-trip tests.
2. The proposed tubelet-atomic GAPPACK is only valid if the instantiated official VideoMAE-S exposes temporal atoms that can be mapped one-to-one through the heavy path and inverse-permuted before shared reconstruction. Freeze the conditional rule: resolve the real temporal tubelet size from the instantiated config/module; prove one-to-one output support. If this is not possible without changing the detector/backbone semantics, GAPPACK must fail closed and be dropped, not approximated. State the exact non-identity construction and prove it is a bijection for general M and L.
3. The first response says `FZ_CONTIG / FZ_GAPPACK_ATOM / JT_CONTIG` distinguishes selector quality and dynamic-budget error. It does not: all three use S0 and fixed M=24. It isolates temporal presentation and frozen-vs-joint semantic-scout drift. Correct the estimands. Selector quality is only judged relative to a compatible immutable uniform-K384 receipt; dynamic-budget value is tested later by fixed/dynamic/K-shuffle/actionness-only.
4. The `0.50 pp`, `-0.20 pp`, and `±1% wall-clock` gates were asserted without a power, noise, or measurement argument. Either justify them from pre-existing immutable variance/cost receipts without using held-out outcomes, or replace them with a principled rule based on pre-registered practical equivalence margins and paired uncertainty. Do not invent precision the existing receipts cannot support.
5. The coverage certificate still needs fully normalized definitions: denominators and zero-mass behavior for start/end/actionness, uncertainty weighting that cannot promote pure ambiguity, physical-gap units, context handling at boundaries, candidate feasibility, and the exact objective/tie rule. Freeze these sufficiently for two independent implementations to return identical selections.
6. Clarify the ordering. We need the smallest implementation and real experiment that can falsify the current input/optimization hypothesis before Query or dynamic M. Query-Bridge can follow only if the base passes. Cycle is conditional; semantic distillation remains deleted. Dynamic M later remains mandatory for the final headline, not silently abandoned.
7. The immutable uniform receipt may be protocol-incompatible. Since duplicate uniform training is forbidden, state honestly: the new triad can still be an internal mechanism experiment, but paper-level non-inferiority remains blocked until a compatible shared receipt exists; do not authorize duplicate training.

## Required final response

Return exactly these sections:

1. `FINAL_DECISION`: exactly one of CONTINUE/REVISE/PIVOT/STOP; this is final, no third round.
2. `ACCEPTED_SCIENTIFIC_KERNEL`: what survives unchanged.
3. `CORRECTED_ALGORITHM`: complete S0 outputs/losses, normalized coverage certificate, deterministic nested CONTIG acquisition, fixed-M and later dynamic-M definitions.
4. `CORRECTED_GAPPACK_CONTROL`: exact bijection, direct equality/round-trip invariants, resolved-tubelet precondition, and fail-closed path; no hashes/checksums.
5. `FINAL_EXPERIMENT_LADDER`: smallest nonredundant full-data experiment first, exact estimands, then conditional SQ/cycle and dynamic-budget stages; no duplicate dense/uniform/random.
6. `STATISTICAL_AND_COST_GATES`: defensible frozen margins and how they were chosen without held-out leakage; if current evidence cannot justify a numeric margin, specify a training-only procedure that freezes it before evaluation.
7. `BUILDER_CONTRACT`: tensors, source labels, gradients/detaches, real heavy execution, physical-time state, checkpoint/resume, configs/launcher/tests and exact fail-closed conditions.
8. `CRITIC_AND_EVALUATOR_CONTRACT`: what independent Critic must verify and what Evaluator PRE_RUN must bind before any N16R4 submission.
9. `REMOTE_EXPERIMENT_CONTRACT`: full canonical THUMOS14, official evaluator, first full seed then expansion, resources, five-epoch recovery checkpoints, final/final-EMA, output receipts, and stop rules.
10. `PUBLICATION_BOUNDARY`: the single paper claim, anti-claims, novelty invalidators and result-to-claim limits.
11. `NEXT_ACTION`: immediately actionable Builder handoff with one clean candidate, not another plan-only loop.

Be concise enough to implement, but do not leave scientific degrees of freedom to the Builder. The goal is a final contract that the Coordinator can accept and execute without another Pro turn.
