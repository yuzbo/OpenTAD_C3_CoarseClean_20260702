---
title: CURRENT_RESEARCH_STATE
version: v013
date: 2026-08-14
stage: FINAL_INNER_MECHANISM_PRO_ADJUDICATION_MATERIAL_READY
author_role: coordinator
github_url: https://github.com/yuzbo/OpenTAD_C3_CoarseClean_20260702.git
base_head_non_duca: a6bdc084cc145c80b6b2c68d0a38f0deea3e8518
supersedes: CURRENT_RESEARCH_STATE-v012 (quarantined transport epoch)
evidence_class: STATIC_PLAN_ONLY / NOT_EXECUTED
material_epoch: LOCAL_ONLY-v013-20260814T172500+0800
---

# Current research state — final inner-mechanism adjudication required

Dynamic budget remains the required core of
`DUCA_HIERARCHICAL_DYNAMIC_PHYSICAL_ACQUISITION-v001`: a train-only outer
policy chooses per-video/window `K` from hard detector utility, paired-boundary
or high-IoU risk, and realized cost. Fixed `K` is only a matched-cost baseline,
attribution control, or fail-closed fallback; it cannot become the final route.

The terminal independent Route-B plan review exposed F1 (no mutually identical
constant-density/canonical-uniform decoder convention) and F2 (no reproducible
K-shuffle nonce, row order, or permutation). The last scientific round is now a
strictly presented—not locally decided—inner-mechanism fork under the same outer
dynamic-K policy:

1. **A:** arbitrary non-contiguous exact-K selection, retaining every selected
   frame's original timestamp and physical coordinate, with a timestamp-aware
   detector-time integration and pre-NMS physical mapping. This is enhanced
   integration, not a pure plugin.
2. **B:** monotone/local physical exact-K transport, retaining physical time,
   bounding stride/displacement, keeping detector internals unchanged, and
   requiring a frozen constant-density-to-canonical-uniform identity.

Both candidates are forbidden to use selected ordinal rank as uniform time. The
only evidence is the static plan
`BUILDER_DUCA_DYNAMIC_INNER_MECHANISM_MINIMAL_CHANGE_PLAN-v001.md`; there is no
Route-B implementation, PRE_RUN, data traversal, pilot, training, official
metric, realized cost, efficacy, novelty support, or paper claim.

The prior v008 Source batch is `UNKNOWN_REMOTE_STATE / QUARANTINED` after one
non-confirming mutation and is neither retransmitted nor relied upon. This v013
state and its paired final-Pro packet are a new local material epoch. Any later
route retained by Pro must use video-disjoint training-population FIT/CAL/HOLD,
official THUMOS14/OpenTAD-AdaTAD evaluation only after firewall/PRE_RUN gates,
the six arms `dense`, `uniform_k384`, `dynamic_A`, `dynamic_B`, `k_shuffle`,
and `no_risk`, plus O1--O4, F-INV, and full-stack-cost stop rules.

The repository root is dirty (275 paths at preparation) and its base HEAD
`a6bdc084...` is SparseHead contamination, not a DUCA experimental identity.
Existing prefix-budget work is historical/negative; the fixed-K density decoder
is prototype-only; no old U/O/R or sealed-replacement surface may be reused.
