---
title: CURRENT_RESEARCH_STATE
version: v012
date: 2026-08-14
stage: FINAL_PRO_ADJUDICATION_MATERIAL_READY
author_role: coordinator
github_url: https://github.com/yuzbo/OpenTAD_C3_CoarseClean_20260702.git
base_head_non_duca: a6bdc084cc145c80b6b2c68d0a38f0deea3e8518
supersedes: CURRENT_RESEARCH_STATE-v011
evidence_class: STATIC_PLAN_REVIEW_ONLY / NOT_EXECUTED
---

# Current research state — final dynamic-route adjudication required

`DUCA_HIERARCHICAL_DYNAMIC_PHYSICAL_ACQUISITION-v001` remains the only
dynamic-budget candidate. Dynamic budget is its required core: the outer policy
chooses per-video/window K and the K-independent inner reader places exactly K
physical samples. Fixed K is only a dense-control, matched-cost baseline,
attribution control, or fail-closed fallback; it cannot become the final paper
route. Candidate A is a paused/negative historical prefix plugin and Candidate
C is not an automatic fallback.

The first independent static review of the immutable Route-B plan returned
`DYNAMIC_ROUTE_B_PLAN_BLOCKED`. This closes the role correction loop but is not
a scientific cancellation of dynamic budget. It exposes two unresolved method
definitions that must be fixed by a final fresh Project Pro decision before any
new plan, code, Critic, Evaluator, PRE_RUN, data, GPU, Slurm, pilot, training,
evaluation, metric, cost result, or claim:

1. At `T_v=768,K=384`, the stated midpoint-CDF inversion plus lexicographically
   smaller squared-error projection produces `(0,2,...,766)`, while the same
   plan calls `(1,3,...,767)` its canonical-uniform identity. The constant-density
   path and fixed-K control therefore have no common frozen decoder identity.
2. The K-shuffle control lacks a frozen nonce/seed, canonical row order, and
   permutation algorithm, so content-to-K allocation and its O4 causal attack
   cannot be reproduced.

These are `STATIC_PLAN_REVIEW_ONLY / NOT_EXECUTED` ambiguity evidence, not
performance, cost, novelty, or efficacy evidence. The root remains a dirty DUCA
working tree whose base HEAD `a6bdc084...` is SparseHead contamination rather
than DUCA scientific identity. Existing production prefix-budget code is
historical/paused; the fixed-K density decoder is prototype-only; the new
hierarchical policy, deploy-visible reader, utility/risk targets, exact-K
transport, six arms, and gates are designed only.

If Pro freezes both definitions and retains the route, all later real-video work
must use training-population-only video-disjoint FIT/CAL/HOLD, the official
THUMOS14/OpenTAD-AdaTAD evaluator, and the sealed six arms (dense, uniform-K384,
learned-K384, dynamic-K, K-shuffle, no-risk). O1--O4 and a full-stack cost ledger
remain unexecuted hard gates; official validation/test remain inaccessible to
route selection. No route execution has been authorized.
