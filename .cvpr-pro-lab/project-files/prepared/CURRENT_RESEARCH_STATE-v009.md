---
title: CURRENT_RESEARCH_STATE
version: v009
date: 2026-08-14
stage: DRAFT
author_role: coordinator
github_url: https://github.com/yuzbo/OpenTAD_C3_CoarseClean_20260702.git
github_commit: 63a726a4aaf48ecbf6780bb196de43a890c6b4df
protocol_revision: a6bdc084cc145c80b6b2c68d0a38f0deea3e8518
supersedes: CURRENT_RESEARCH_STATE-v008
evidence_class: BLOCKED_PRE_RESULT
---

# Current research state — density reachability protocol frozen

`PRO_DUCA_DENSITY_REACHABILITY_DECISION-v001` is the accepted local fresh-Pro
decision and is `REVISE`. It rejects an official-validation GT-boundary-density
versus exact-uniform comparison as evidence of deployable reader headroom. That
comparison would use privileged labels for route selection and cannot establish
deploy-visible reachability. It authorized exactly one author-only follow-up:
freeze a training-population-only, video-disjoint FIT/CAL U/O/R reachability
protocol. It did not authorize production code, data access, PRE_RUN, or an
experiment.

The independently rebound Evaluator produced
`DUCA_DENSITY_REACHABILITY_PROTOCOL-v001` at a clean detached
`a6bdc084cc145c80b6b2c68d0a38f0deea3e8518` evaluation-only binding. The
protocol is `FROZEN_AUTHOR_ONLY_NOT_EXECUTED` and `PRE_RUN_NOT_READY`. It
requires official validation to remain inaccessible, a deterministic
training-population video-disjoint FIT/CAL split, one FIT-only terminal detector,
a FIT-only reader, and a one-time CAL comparison of U (canonical exact uniform),
O (privileged GT-boundary diagnostic only), and R (deploy-visible browser-memory
reader with no GT, teacher, cached prediction, or detector-result input).

The frozen decision statistic is 10,000 nonce-seeded paired video-cluster
bootstrap resamples that recompute the official evaluator over pooled predictions;
per-video AP averaging is forbidden. The MWE is +0.50 pp, with predeclared
ADVANCE/HOLD/KILL conditions and high-tIoU protection. These are a future
training-population mechanism gate, not official-validation evidence, a result,
or a paper claim.

The only density implementation evidence remains an untracked dirty-worktree
prototype with local compile/fixture checks and an incomplete N16R4 pure-Python
geometry probe. It remains inadmissible as production, PRE_RUN, evaluator, or
performance evidence. Production reader/config/coordinate transport/U-O-R
evaluator/FIT-CAL manifest/bootstrap/launcher wiring does not exist. No data,
GPU, Slurm, metrics, mAP, cost result, official-validation access, or training
has occurred under this reachability protocol.

The next required decision is narrow: after this state update and the frozen
protocol are confirmed as exact Project Sources, a fresh Project Pro turn must
either specify one bounded Builder tracked production package followed by one
Critic PRE_RUN review and the existing Evaluator protocol, or STOP/REVISE. It
must not start another theory round, relax the training-population/held-out
boundary, or authorize an experiment implicitly.
