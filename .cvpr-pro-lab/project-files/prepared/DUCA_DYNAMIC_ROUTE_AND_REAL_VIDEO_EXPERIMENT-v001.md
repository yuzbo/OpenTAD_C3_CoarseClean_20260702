---
title: DUCA_DYNAMIC_ROUTE_AND_REAL_VIDEO_EXPERIMENT
version: v001
date: 2026-08-14
stage: DESIGNED_ONLY
author_role: coordinator
evidence_class: ARIS_CPR_MATERIAL_PREPARATION_NO_EXECUTION
---

# DUCA Dynamic Route and Real-video Experiment Packet

## Decision requested from fresh exact-Project Pro

Given the evidence boundary below, should Route B receive only a bounded
Builder -> independent Critic -> existing Evaluator PRE_RUN authoring sequence,
or should it STOP / receive one explicitly bounded REVISE disposition? No
answer may authorize data access, official validation/test access, GPU, Slurm,
pilot, training, evaluation, metrics, cost results, or a paper claim.

## Provenance and negative boundary

- Root cwd is `E:/DeskTop/TAD/OpenTAD_C3_CoarseClean_20260702`; common-dir is
  `.git`; base HEAD `a6bdc084...` is a SparseHead commit, not a DUCA revision.
  DUCA evidence is named dirty working-tree surfaces plus the Wiki. SparseHead
  Route-T and `pc_ot_mras_*` tracked family are not DUCA evidence.
- Existing production dynamic-budget code:
  `opentad/models/duca/dynamic_budget.py` provides
  `PrefixMarginalUtilityBudgetController` and `lambda_dual`; historical
  `opentad/models/duca/acquisition.py` consumes it through
  `budgeted_center_radius_decode`. This dynamic_must/selected-axis route is
  paused/negative, not a final route.
- Prototype-only primitive: untracked `density_decode.py` is a frozen fixed-K
  physical exact-K decoder without a production reader/evaluator/launcher.
- Discussion only: RIME/outer-budget-inner-transport, hard detector utility,
  paired-boundary/high-IoU risk, nestedness choice, and deploy-visible dynamic
  reader. No efficacy evidence exists.
- The former U/O/R package and sealed replacement are terminal negative
  implementation evidence; no third correction, recheck, or reuse is allowed.
- Fixed K is baseline/attribution/fallback only, never the final paper route.

## Candidate routes and ARIS round-1 proposal

1. A, original prefix-marginal plugin recovery: rejected as final route because
   selected-rank geometry and greedy center-radius are known failure modes and
   the mechanism confounds which frames with how many frames.
2. B, hierarchical Dynamic DUCA: outer per-video/window K chooses a finite
   budget from train-only hard detector utility minus paired-boundary/high-IoU
   risk and measured cost; inner physical exact-K transport chooses frames at
   that K and maps proposals back before unchanged official NMS. It predicts
   budget heterogeneity and pair-risk protection that uniform or matched-K
   baselines cannot produce. This is the conditional recommendation only.
3. C, realized-cost Lagrangian density: couples `lambda_dual` to a density
   decoder, separating quantity price from density shape. It is the stronger
   deliverable fallback if B has no dynamic headroom or unusable nestedness.

Novelty invalidators include AdaFrame, MGSampler, AdapTok, AdaFocusV3, SMART,
TAPS, progressive block dropping, inverse-CDF sampling, budget scorers, and
ILP. The only potentially publishable claim is the still-unverified combined
mechanism: train-only hard budget-conditional utility plus paired endpoint/high
IoU risk, physical exact-K transport, and batch-invariant realized cost without
changing detector internals. Pro must reject the route if that combination is
not materially distinct or isolatable.

## Required O1--O4 kill gates, before real route evidence

- O1: frozen-detector dynamic Oracle headroom at matched realized mean cost
  versus the best fixed K. No headroom kills the dynamic main route.
- O2: decoder-family regret across strict nested, weak-overlap, and independent
  exact-K families. Excess strict-nested regret removes nestedness.
- O3: train-only hard detector-utility `G_rank` predictability. Failure removes
  the utility head.
- O4: paired-boundary/high-IoU risk must beat simple actionness/transition
  signals. Failure removes the risk contribution.

Thresholds must be frozen only after clean baseline variance and video-level
power analysis; O1--O4 are not executed and have no results.

## Real THUMOS14 protocol if, and only if, Pro authorizes later PRE_RUN

- Data: training-population-only video-disjoint FIT/CAL/HOLD. Official
  validation/test must remain inaccessible to route selection, epoch/K/seed
  choice, teacher labels, or utility/risk construction.
- Strongest comparators: released OpenTAD/AdaTAD dense baseline, clean native
  uniform fixed-K, learned fixed-K, dynamic-K B, K-shuffle, and no-risk. All
  must retain the same detector, loss, pre-NMS physical-time mapping, official
  evaluator, split, seed policy, and matched realized cost accounting.
- Pilot: one real-video development seed only after structural gates; it is
  excluded from formal statistics. Formal training is 6000 successful detector
  updates with preregistered fresh seeds only after pilot success.
- N16R4: Slurm allocation only, `source /etc/profile` before module setup,
  `--gpus=1 --cpus-per-task=6`, and in-process `cuda:0` without overriding
  `CUDA_VISIBLE_DEVICES`. No login-node training.
- Report full-stack cost: decode, preprocess, H2D, coarse probe, outer scorer,
  inner decoder, heavy backbone, head, NMS, plus hard-label/refresh GPU-hours.
- Stop: no O1 headroom, O2/O3/O4 failure, development result no better than
  best fixed K at matched cost, high-IoU/short-action degradation, padded Kmax,
  or no net full-stack saving ends the efficiency/paper claim.

## Bounded future authoring scope if Pro says AUTHORIZE_PRE_RUN

Only additive dynamic-K policy, deploy-visible reader, exact-K integration,
train-only utility/risk construction, six-arm configs, O1--O4 gate tools, and
one closed no-data launcher/evaluator protocol. A single independent Critic
then attacks those surfaces. Existing Evaluator performs structural intake only
after Critic PASS. Any BLOCKED result stops the sequence; no correction loop is
pre-authorized.

Evidence state: designed/discussed only; no Builder package, PRE_RUN, pilot,
training, performance, cost, or claim exists.
