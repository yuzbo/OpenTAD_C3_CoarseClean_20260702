# Evaluator — one exact PJST-D1 terminal-finalizer environment recovery

The immutable failure diagnosis shows both prior evaluation jobs exited before inference because `DUCA_STAGE1_CHECKPOINT` was absent while loading the shared Stage-2 config. No prediction or partial artifact exists. This message consumes the one same-task infrastructure recovery and authorizes no scientific change.

Using exact clean remote commit `4204937a933c7a48854b623efefc7fd662e98805`, submit exactly one OFF replacement and one ON replacement with all prior bindings unchanged plus these immutable Stage-1 environment bindings:

- `DUCA_STAGE1_CHECKPOINT=/data/run01/sczc063/yuzibo/duca_h65_stage1_uniform384_cycle6_61397c0e_20260823/gpu1_id0/checkpoint/epoch_29.pth`
- `DUCA_STAGE1_CHECKPOINT_SHA256=bcbc877c204a1ce7778f559be0b218295223367983450274671b17356e5be4e3`
- `DUCA_STAGE1_CHECKPOINT_EPOCH=29`

Before submission, read the exact resolved config interface and verify these are the only missing required Stage-1 variables; retain the canonical pretrain override and every previous annotation/class-map/video/config/checkpoint-state/output binding. Verify the checkpoint path and SHA read-only. Use fresh output roots `off/terminal_eval_v2` and `on/terminal_eval_v2` so no failed-job collision or ambiguity exists.

After both replacement jobs are authoritatively accepted, submit exactly one bundled bootstrap/finalizer job from commit `4204937a` with `afterok:<new_off>:<new_on>`, using their exact `result_detection.json` paths, the frozen 16-shard/10,000-draw contract, bounded `--cpus-per-task=4`, and fresh `bootstrap_shards_v2` / `bootstrap_v2` roots. Do not submit an array or separate merge job.

Return immediate `START` with exact commands, new job IDs, dependencies, output roots, revision/clean proof and current states. Then monitor the exact three-job DAG to terminal and write the full final Evaluator receipt. Unknown submission state gets one read-only scheduler reconciliation; do not duplicate any accepted job.

Forbidden: training/resume/update, alternate checkpoint/state key, new seed/arm, code/config change, data/split/evaluator/NMS change, post-hoc threshold or claim change.

