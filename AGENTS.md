# GeoSparse on official AdaTAD

Fixed R4541c641 review follow-up: read docs/evaluation/fixed-review-followup-20260908.md. This independent repair branch changes N02 acquisition/evidence validity and N03/N04 plots, and names N05 research pairs equal-Heavy-cost. It is not frozen production M. Keep existing six runs and checkpoints under M; no hot edits or automatic restarts. Existing C20 replay245503 and A fixed-q diagnostic1280030 must not be duplicated. New measurement tools must load the intended frozen model explicitly. A/B uniform-T cannot alone isolate selector effects against ST primaries.

The base is official sming256/OpenTAD commit 346d09d19e2091372cec48172dbe40f7b28bdee6. Keep `opentad/`, the inherited model/dataset configs, and original train/test entry points unchanged. Implement research routes in `geosparse_ext/` and focused tests.

User-mandated protocol: all 200 THUMOS training videos; all 211 official test videos / 792 test windows at every formal validation; no internal training holdout. Official defaults are 768 input frames, 160px, 768 detector grid, global batch 2, warm-up 5, cosine horizon 100, training end epoch 60. Run seed 0 first. Checkpoint selection uses full-test EMA mAP every 5 epochs and must retain full predictions and provenance.

Do not resume withdrawn 180-video / cosine-60 runs. Historical manifests and results remain under the external execution package. Current focused and full amended manifests have distinct provenance and new IDs. No mAP or Oracle promotion gates.

Validate changes with the GeoSparse detector, native execution, runtime, training validation, and official protocol tests. Production capabilities additionally require the actual GPU native shapes, forward/backward, optimizer, EMA, and original source output/gradient checks.

Keep generated data, weights, run logs and plots outside this repository. Remote N16 writes stay within `/data/run01/sczc063/yuzibo`; N16 allocations use the authorized physical GPU1 / container GPU0 mapping. A100 writes stay in the user's isolated `geosparse_tad_20260907` directory and use its actual Slurm allocations. No training on login nodes.

Audit repair amendment: read `docs/GEOSPARSE_AUDIT_REPAIR.zh.md`. Frozen 902fa05 runs and their queues are retained under AUDIT_HOLD and must not be resumed as repaired-protocol results. New protocol `geosparse-audit-repair-20260908` has new task IDs, an explicit epoch artifact contract, and its own work roots. Prioritize seed0 full A/B/C with both fixed .5 and dynamic budgets, then controls; no performance gates. Keep original official training and `geosparse_dense_control` distinct.

User authorization: if subsequent actual experiment performance fails the agreed objective, agents may discuss implementation/results and diagnose failure using Computer Use to operate ChatGPT Pro in ixBrowser without asking again. Use that UI route, preserve evidence and adopted changes, and follow the current data/training protocol. Do not substitute an API/another browser or claim a consultation that did not occur. Early low mAP alone is not a final failure.

Documentation: read docs/README.md and the 2026-09-08 localization-value amendment in docs/project/paper-plan.md. MoD plus the same native-grid TIA is a required strong comparison; full state, gather/scatter and dense TIA are not exclusive innovations of A. Current M implements ordinary TAD-loss acquisition, not the proposed interval-risk/equal-cost-swap extension. The seven entries in docs/evaluation/mod-tia-proposals.json are research designs, not executable or submitted jobs. Preserve existing A/B/C dynamic and fixed-.5 seed0 runs and task identities; do not relabel them as A2, duplicate them, or change deployed model snapshots for a paper revision.

B/C/LITE amendment: also read docs/project/prior-art-bc-lite.md, docs/methods/evidence-and-shared-updates.md and docs/evaluation/bc-lite-comparison-plan.md. Treat A/B/C as three executors of one research principle, not three newly invented architectural primitives. Current C already implements signed ordinary-loss coarse-to-fine probes; it does not implement the proposed interval-risk/swap or mode-switch interventions. Current B has native384 receiver/TIA followed by output768; anchor support is not the full encoding dependency set. LITE-style task-value prediction and adaptive budget are strong prior art. New receiver/merge/LITE comparisons and four figures are designs with missing implementations/data, not new submitted jobs. Do not add smoothing or other modules merely to fit an unproven narrative.

Dynamic-budget audit: read docs/evaluation/dynamic-budget-audit.md. M has PG cost penalties and dual updates, but evaluation uses an unconstrained budget argmax. A configured .5 target and a training cost EMA do not establish .5 inference cost. Pair claims with the same EMA/epoch's actual validation plans, distinguish eligible-full from padded-full Heavy MAC, and retain partial coverage labels. Full-budget checkpoints may retain their accuracy at the actual cost but cannot support .5-budget efficiency. Do not silently alter frozen runs or claim a new constrained allocator has been deployed. Prospective test-based tuning is now explicitly authorized under docs/evaluation/test-development-amendment-20260908.md and must be identified as test-tuned development. Existing primary training continues under its original identity; this finding does not create performance gates for other routes.

SCI3 intake: read docs/evaluation/sci3-integration.md and sci3-intake.json in the same directory. These record the v3 Markdown's 30 research questions, 22 figures and 6 tables, not the missing original 805-request manifest. New shared research code lives in geosparse_research/; it reuses forced RoutePlan execution and the official AP function without modifying M. The first pair runner measures source R0 in eval mode with full recomputation, not R1/query risk or a trained swap router. Preserve existing full200/seed0/full-test best until the v3 protocol ambiguity is resolved. Never call in-sample probes held-out, or source-style adaptations complete reproductions without matching evidence. New tests: python -m pytest tests/test_sci3_interventions.py tests/test_sci3_bootstrap.py -q. CPU synthetic-input correctness is not production-GPU or scientific validation.

Fixed-budget diagnostic: read docs/evaluation/fixed-budget-intervention.md. Independent Slurm1280030 runs the completed dynamic-A final E40 EMA with inference fixed q=.5 after benchmark1279571 releases g0087; first a GPU precheck, then all211/792. Code7ea094f4 passed six focused CPU tests. Model imports remain frozen M; no training or allocator was changed. It records cost and predictions from the same forward and reuses training-derived risk thresholds. It is not a learned constrained method, q=.5 is not MAC=.5, and submitted/pending is not a measured result. Do not duplicate this job or the completed original A evaluation. Focused test: python -m pytest tests/test_sci3_fixed_budget_diagnostic.py -q.

1280030 is held for primary resource priority while fixed A is QUEUED_RESOURCE. Release the same ID only once benchmark1279571 ends and fixed A has a real running allocation or has completed; this is a resource condition, not a performance gate. Keep resource_hold.json and the original submission record; do not resubmit this diagnostic.

2026-09-08 explicit user steering: use best checkpoint and permit test-set hyperparameter development. Read docs/evaluation/test-development-amendment-20260908.md and performance-diagnosis-20260908.md. This overrides earlier no-test-tuning language for prospective development, not the frozen identity of existing runs; retain full200 training and label test-tuned results honestly. Test GT remains outside optimizer/utility training. A dynamic best is E40, not E60; late test regression with decreasing train loss and B's weak same-epoch fixed-budget performance require separate diagnoses. C E15 outperforms A E15 and is not a completed failure. Do not infer gradient domination from large negative actor-loss scalars. Pro consultation was attempted but blocked by Computer Use kernel-asset initialization; no consultation occurred.

Training-gradient diagnostic: `geosparse_research/training_gradients.py` measures task/actor/critic/acquisition/actionness and direct-total gradients from the same forward, preserving reentrant checkpoints and the original global clip. Read docs/evaluation/training-gradient-diagnostic.md. Code18281bac passed10 focused CPU tests in a clean independent checkout; real checkpoint/batch CUDA evidence remains pending. Supply actual ordinary training state, RNG, epoch/minibatch, microbatch, normalizer and AMP scale; do not call EMA inference weights a replay of the original training step. The function restores state and performs no update. Never infer which loss dominates B before those measurements, or divide PL log-prob by K as an unregistered shortcut.

Ordinary-checkpoint diagnostic entry `training_gradient_diagnostic.py` was added in65190737 and passed6 new clean remote CPU checks (56.05s). Run as a file so model imports come from the existing run's frozen M bindings; measurement imports come from the independent research source. It restores ordinary epoch checkpoint/RNG/normalizer/GradScaler and uses the next epoch's first training batch, without updates. Planned B dynamic E20 probe must not be called an arbitrary historical-batch replay, a full-training gradient distribution, or a completed GPU measurement. Preserve absent sampled actor/acquisition terms instead of altering counters or forcing a plan.
