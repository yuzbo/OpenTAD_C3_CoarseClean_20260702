# 用户更新：完整动态方法与固定50%并行

这次更新覆盖审查快照55d5429中“当前五个focus配置”的执行优先级描述。模型源码仍为902fa05，原始审查快照保持可追溯。

用户要求完整方法包含动态预算，并明确固定50%也继续。因此当前先进行A/B/C各两个版本，共六项seed0主实验；官方AdaTAD继续。B-full和额外稠密对照延期，其他预算曲线及消融实施延期。具体规则、任务ID和预算口径见 [完整执行修订](FULL_METHODS_FIRST.zh.md)。

本目录保存当前外围部署器、动态预算头真实梯度检查、动态manifest及执行状态。动态方法必须先通过真实microbatch的预算头/gain梯度、acquisition、optimizer和成本控制器检查，再经过原有精确配置GPU预检才能正式入队。GPU预检不是论文实验结果；当前全量验证中的早期mAP也不是60epoch最终性能。

`bindings.*.public.json`和status中的机器路径已脱去，不能作为生产绑定运行。固定50%继续使用原有不可变代码与训练记录，未被重启；动态版本独立从识别预训练开始。审查者应继续核查902fa05的模型本体，也应纳入这里的部署与检查代码，不执行其中的生产命令。
