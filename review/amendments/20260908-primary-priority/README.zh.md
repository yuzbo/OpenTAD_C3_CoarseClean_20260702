# 主方法优先，部署后推进补充实验

2026-09-08用户更新：A/B/C完整创新方法是首要性能验证目标；在其部署或开始训练后推进其他实验；4090/N16R4和A100同时部署。继续seed0、官方全数据协议、60epochs，不依据mAP建立门槛。

六主实验全部已部署。A100固定B/C保留原Slurm ID，分别用after依赖让各自动态完整方法先开始；正在训练的A固定50%不重启。第一批补充为B-full、同结构稠密对照和A/B/C uniform，5项训练加各自evaluate/benchmark共15条任务。补充队列nice10000，依赖本服务器主作业开始，不是afterok。B-full/DENSE复用同一902fa05、同一配置的已通过预检，无既有训练被重复；uniform等待各自预检。

本目录附部署器、优先级设置器、监控器、补充manifest、脱去机器路径的绑定和实际状态。`secondary_queue.py`是外围调度代码，调用未改变的生产902fa05模型与队列；不要把审查文档commit当成新的训练模型来源。已有源码与官方核心均不变。

状态快照时间见status.public.json。当时A固定50%已完成9/60轮；A动态因N16共享盘仅余约7GiB而未提交，A100主方法仍在集群优先级队列中。两个服务器均已部署不代表两端均已开训。原始200train/211test数据保留；正在把现有A检查点复制到A100，未完成的传输不算备份验收成功。

检查预检失败与调度阻塞时，应保留日志并在诊断后显式重试；不能盲目反复提交。实际性能只接受真实全量验证产物，预检、排队和部署成功不计为科学结果。完整协议见FULL_METHODS_FIRST.zh.md。Pro审查尚未执行。
