# 2026-09-08：全测试集 best 与测试集开发调参授权

用户本轮明确要求：“选‘最佳 checkpoint’且按测试集调参，当前部分实验性能太差，远低于原始 adatad 方法，请你分析原因。”本修订覆盖旧文档中禁止本用途测试集调参的条款；无须再次询问相同授权。

1. 保留全部200训练视频、211测试视频/792窗口、seed0优先、60轮及现行官方基座。GeoSparse仍按全部正式验证节点的EMA average-mAP选best；保存所有节点，而不只保存best。官方原版保持其原版验证节奏，独立训练与发布权重复测继续分开。
2. 此后允许将完整测试集用于超参数开发与checkpoint选择。新增测试驱动调整须写明修改参数、依据、选择过程和结果；报告明确标记“测试集参与调参的开发结果”。这类数据不再构成未参与开发的独立泛化验证，不能把其bootstrap当作消除选择偏差的办法。
3. 本授权不自动变成把测试视频/GT加入optimizer、utility标签或训练batch的授权。当前训练仍只用200训练视频；不重新划出20视频holdout。
4. 冻结生产M=b70ae056及其既有运行身份。不得把新超参数写入正在运行的原实验，不覆盖原checkpoint，不把旧结果改名成新方法。新的训练修改以明确的新配置/运行登记；纯评价干预沿用自己的诊断身份。
5. 仍以A/B/C完整主方法为优先，固定q=.5与动态两组、两服务器并行。原实验不重复；用既有B-full、统一dense和uniform控制解释差距，不为一项低分重跑全部矩阵。没有实际预算约束的动态结果不能因mAP较高升级为50%成本结果。
6. 先完成原因诊断。学习率、PG/critic/CF权重、探索或receiver调整分别对应具体假设；不凭负的actor-loss标量直接认定梯度错误，也不将官方cosine100/end60认定为协议错误。修改涉及实现时先做对应真实检查，再部署。
7. ixBrowser中的ChatGPT Pro讨论授权继续有效，必须用Computer Use并验证实际Pro入口。2026-09-08本轮Computer Use两次初始化（含reset）均报“failed to write kernel assets / 找不到指定路径”，统一CUA入口也报同错；本轮尚未完成Pro咨询。不得用API、别的浏览器/模型代替，也不得报告已咨询。

本修订是从现在开始的实验开发规则，不把之前固定协议结果倒写成测试调参结果。分析见[低性能诊断](performance-diagnosis-20260908.md)。
