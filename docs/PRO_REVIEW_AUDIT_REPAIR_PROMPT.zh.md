# 审计修订代码的复核 Prompt

请作为严厉且基于源码证据的研究实现审查者，检查当前 Git 提交的 GeoSparse-TAD 修复。先读取 docs/GEOSPARSE_AUDIT_REPAIR.zh.md，再与审计对象902fa05和官方基座346d09d1做diff。用户提供的原始独立报告列出I01–I15，请逐项核销，避免仅凭新文档或测试名字接受修复。

优先检查：

1. 原frame mask是否真正到达ActionFormer的目标分配、loss、预测，而非只修encoder输出。768输入、767有效、query384的定义是否一致。
2. B的receiver→native384 TIA→query768顺序，跨parent的轻时间交互与parent内Heavy attention是否分清；B-full是否仍被诚实描述为新架构控制。
3. 同一次ImgAug像素操作和坐标记录是否确实保持官方增强及下一次RNG；旋转/剪切的四角、无证据和尾部是否正确。锚点支持不能冒充全部编码依赖或独立缓存键。
4. EMA进入/退出验证是否恢复所有冻结和可训练状态。一次失败验证、补测、best变化、独立导出、benchmark、绘图能否形成正确的完整准入和身份链。
5. 160/224/112下成本参考、C全粗非零成本和hardware UUID是否正确。硬件和精度是否绑定同一具体权重，是否混用4090/A100。
6. 新协议ID、full/focus保存点、诊断依赖、已阻塞group7/L2语义是否清晰；测试是否真的能证伪报告缺陷。
7. 官方训练与统一dense control在local-positive归一化、AMP overflow推进和选点次数上的差异是否被隐藏。全200训练、全211测试选EMA best是用户授权协议，不得擅自恢复holdout；仍须披露选择偏差。

给出每项“已解决/部分解决/未解决/原指控不成立”，附具体file:line、可达条件和最小反例。只对源码或真实产物支持的错误下结论，正确处明确认可。CPU测试通过不等于生产GPU验证，更不等于最终mAP或加速；缺结果说明证据尚缺，不能据此说方法被证伪。不要要求重跑所有实验：训练目标受影响、仅测量受影响、未开始作业和官方原版分别判断。

最后列出阻止恢复正式训练的项目、阻止发布结论的项目，以及修复是否引入新的核心矛盾。不要发明实验结果或将CoDA/mTAN/MSViT已有原语当作首次提出。
