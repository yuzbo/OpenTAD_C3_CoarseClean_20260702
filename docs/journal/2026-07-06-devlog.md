---
date: 2026-07-06
status: active
scope: GPT/Pro review record and absorption for C3/GAS-VT/detector-aware/TrueTime routes
---

## GPT/Pro review record: WARN approaching HOLD

Source: user-provided review attachment `5edf832c-35d8-414e-9e4a-a2389cb5db62/pasted-text.txt`.

Overall verdict recorded: the current four-route plan is not yet fully implemented or proven enough for paper claims. It should be treated as engineering prototypes plus locked validation gates until detector mAP and leakage/time-mapping audits pass.

## Route status recorded

1. C3 / GAS-VT strict ledger:
   - Engineering route exists and can support Stage-1 detector mAP closure.
   - `constrained_topk(max_unselected_hole)` is a hard decoder repair, not pure learned acquisition.
   - Correct claim wording is learned score plus explicit gap-constrained decoder.
   - Required ablation: pure topK vs topK+gap decoder, GAS-VT vs GAS-VT+gap decoder.

2. Detector-aware selector:
   - Skeleton exists: teacher utility conversion, selector train/apply, ledger validation, launcher.
   - Missing proof: dense AdaTAD teacher utility artifact must be train-only, provenance-locked, and stripped from val/test application.
   - No detector-aware superiority claim is allowed until train-only teacher utility and full mAP are proven.

3. TrueTime / ST joint selector:
   - Review judged the earlier state as smoke proof only, not full AdaTAD/ActionFormer end-to-end.
   - Absorbed update: after this review, a stronger tiny real `ActionFormer.forward_train` gradient proof was added on the TrueTime branch, but it is still only a synthetic precheck, not full sparse AdaTAD mAP or physical-grid training.

4. Curriculum / bilevel:
   - Still a research design and training schedule, not a closed experimental result.

## P0 / P1 blockers absorbed

- P0: val/test must never use GT, teacher utility, oracle boundary/action, raw prediction cache, or dense teacher logits.
- P0/P1: sparse ledger positions must remain original local dense indices, not selected-rank indices.
- P0/P1: OpenTAD/AdaTAD post-processing and point generation must preserve true time coordinates, feature stride, fps, and window offset.
- P1: gap loss is only a surrogate; decoded ledger validator is authoritative for `max_gap` and `p95_gap`.
- P1: fixed 384/768 may become effective budgets on short windows; ledger must record requested/effective/selected counts.
- P1: dynamic budget may collapse into fixed K; require budget distribution, entropy, unique K, std, and same-mean fixed baseline.
- P1: hard gap decoder must be exposed as decoder metadata, not hidden behind `uses_uniform_fill=false`.
- P1: validator must fail closed and recursively reject forbidden leakage keys.
- P1: GAS-VT must prove no uniform scaffold and no residual uniform fallback.
- P1: TrueTime/ST cannot claim end-to-end from toy or single-batch gradient norm; it needs real detector integration, irregular time mapping, anti-collapse losses, and mAP.
- P2: full matrix requires dense, uniform, random multi-seed, p_action, delta, GAS-VT, detector-aware, oracle upper bound, and ST/curriculum variants.

## Concrete implementation requirements absorbed

- Add decoder metadata:
  - `selector_score_source`
  - `decoder_constraint_kind`
  - `is_pure_learned_topk`
  - `uses_uniform_scaffold`
  - `uses_uniform_fill`
  - `max_unselected_hole`

- Add budget contract:
  - `requested_budget`
  - `effective_budget`
  - `selected_count`
  - `valid_len`
  - `dense_len`
  - `short_valid_ratio_applied`

- Validator must recompute and fail on stale metadata:
  - sorted selected positions
  - duplicate positions
  - out-of-range positions
  - selected/effective budget mismatch
  - `selected_positions_unit != local_dense_index`
  - `max_gap` and `p95_gap`
  - forbidden payload in nested rows

- Detector-aware teacher utility must be:
  - train split only
  - generated from a locked dense AdaTAD teacher checkpoint/config
  - summarized by artifact manifest and sha256
  - stripped from all deployed val/test ledgers

- TrueTime/ST must eventually provide:
  - real `SingleStageDetector`/ActionFormer wrapper
  - hard forward and straight-through backward
  - original time index metadata
  - irregular point generator or equivalent true-time mapping
  - budget, entropy, gap, and anti-collapse losses
  - forward-test hard deterministic selection
  - full mAP evidence, not only precheck gradient proof

## Experimental gates absorbed

Week 1:
finish trusted Stage-1 ledger validation, time mapping audit, no-leakage audit, and small-split sanity.

Week 2:
run dense/uniform/random/p_action/delta/GAS-VT full mAP matrix, including high-IoU metrics and dynamic-budget diagnostics.

Week 4:
close detector-aware selector with train-only dense teacher utility, teacher payload stripping, utility ablations, and detector mAP.

Week 8:
implement and test ST joint/curriculum with real detector loss feedback, irregular time mapping, collapse controls, and full mAP.

## Current absorbed position

No paper claim is allowed yet. The next defensible claims must be earned in order:

1. Sparse strict ledgers are safe and time-correct.
2. GAS-VT improves detector mAP over uniform/random/p_action-only under the same budget.
3. Dense AdaTAD teacher utility trains a better acquisition policy than p_action-only without val/test leakage.
4. ST hard selector plus detector joint training can preserve or improve high-IoU TAD mAP without selector collapse.

---

## Pro review record: GAS-VT Stage0/1 commit 53124a2

Source: user-provided review attachment `4cde7395-f9d0-465e-9b01-847bbf020d7e/pasted-text.txt`.

Full raw record copied to:

- `docs/methods/reviews/2026-07-06-gas-vt-stage01-53124a2-pro-review-raw.txt`

Structured absorption record:

- `docs/methods/gas-vt-stage01-53124a2-review-absorption.md`

Absorbed verdict: commit `53124a2` closes the GAS-VT Stage0/1 engineering chain only as an offline sparse-ledger pipeline. It must not be called end-to-end or detector-aware. Allowed wording is: GAS-VT policy checkpoint generates strict deployable ledgers that AdaTAD can consume for full training/eval.

Absorbed blockers:

- No end-to-end claim until selector is inside AdaTAD forward and detector loss reaches selector.
- No detector-aware claim until dense AdaTAD teacher utility or detector loss supervises selector.
- GAS-VT train supervision uses train GT action/boundary/budget targets and must be described honestly.
- Hard gap repair is not uniform fill, but it is still hand-coded decoding and needs metadata plus ablation.
- `cvar_max_hole_loss` is only a surrogate and should not be confused with validator max-hole.
- Paper-ready experiments require dense, uniform, random, p_action, delta-p_action, GAS-VT pure top-k, GAS-VT gap repair, dynamic matched-K, and oracle/teacher upper-bound comparisons.

Next absorbed action order:

1. Finish current `gas_vt_fixed_384` full AdaTAD mAP.
2. Run urgent `uniform_384` and `p_action_topk_384` baselines with the same AdaTAD config.
3. Only expand to fixed_768/dynamic/no-repair/multi-seed if fixed_384 is competitive.
4. Implement Stage2 dense AdaTAD teacher utility with train-only supervision and stripped deploy ledgers.

---

## Pro review record: DUCA target commit 5f87baf review blocked

Source: user-provided review attachment `cb526d60-d6a5-408f-90f8-bf4a53a4ee6c/pasted-text.txt`.

Full raw record copied to:

- `docs/methods/reviews/2026-07-06-duca-5f87baf-review-blocked-raw.txt`

Structured absorption record:

- `docs/methods/duca-5f87baf-review-blocked-absorption.md`

Absorbed verdict: this is a valid external review gate, but not a valid line-by-line code review of the target implementation. The reviewer could access the public repository, but the requested commit `5f87baf` and branch `codex/gas-vt-stage23-detector-aware-20260706` were not visible on GitHub, so the reviewer correctly refused to infer implementation quality from adjacent branches.

Absorbed status:

- `TARGET_COMMIT_VISIBLE=NO`
- `TARGET_BRANCH_VISIBLE=NO`
- `LINE_BY_LINE_REVIEW_DONE=NO`
- `PRECHECK_ONLY_ALLOWED=NO` for this target review evidence
- `FULL_TRAINING_ALLOWED=NO` for this target review evidence
- `PAPER_CLAIM_ALLOWED=NO`

Local interpretation: this review proves a GitHub synchronization/provenance failure, not a source-code PASS/FAIL. It does not prove Stage2/3/4 are absent locally, and it does not verify they are correct. A repeat external review must be run after pushing a fixed reachable commit or providing a fixed diff/zip bundle.

---

## Pro review record: DUCA target commit 5f87baf visible code review

Source: user-provided review attachment `b7ed930b-f3f1-4c07-952d-1fb81e39f0fd/pasted-text.txt`.

Full raw record copied to:

- `docs/methods/reviews/2026-07-06-duca-5f87baf-pro-review-raw.txt`

Structured absorption record:

- `docs/methods/duca-5f87baf-pro-review-absorption.md`

Absorbed verdict: this is a valid external code and experiment-route review because the reviewer confirmed fixed commit `5f87bafc7c63a525ea7c91b7ef4b76b310cc86cf` and key source blobs were visible. It supersedes the earlier visibility-blocked review as the actionable Pro review for `5f87baf`.

Absorbed status:

- engineering implementation: HOLD / WARN
- experiment evidence: FAIL
- paper claim readiness: FAIL
- Stage1 completeness: about 60-65%
- Stage2 completeness: about 45-55%
- Stage3 completeness: about 20-25%
- Stage4 completeness: about 10-15%

Absorbed P0 issues:

1. `single_stage.py` / `two_stage.py` do not integrate the TrueTime selector, so end-to-end detector-loss-to-selector training is not proven.
2. Selected-axis predictions are not explicitly remapped to physical/true time before post-processing/NMS/mAP.
3. TrueTime metadata records selected valid length as dense valid length.
4. Dynamic budget is rank-derived and not a train-split calibrated marginal-gain policy.
5. Deploy forbidden payload removal/checks are not recursive and miss several teacher/proposal/prediction-cache keys.
6. Detector teacher utility proposal-to-dense-point mapping lacks strong axis/fps/stride/window-offset provenance gates.

Absorbed immediate fix order:

1. Recursive no-leakage validator across apply/convert/validate.
2. TrueTime selected-valid-length metadata fix.
3. Selected-axis to true-time segment remap before detector post-processing.
4. Detector forward integration for TrueTime selector.
5. Real detector-loss gradient proof with selector parameter grad norm.
6. Train-split calibrated marginal-gain dynamic budget.
7. Separate positive utility gain and negative observation risk.
8. No-repair vs gap-repair metadata and ablation tests.

Claim lock remains: no sparse-acquisition mAP improvement claim, no detector-aware proof claim, no end-to-end claim, no calibrated dynamic-budget claim, and no CVPR-ready claim until full mAP matrix and end-to-end detector evidence exist.
