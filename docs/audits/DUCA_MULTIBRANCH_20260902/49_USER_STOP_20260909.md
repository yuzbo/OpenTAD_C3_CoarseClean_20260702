# 六路线实验停止记录

用户命令：终止本任务负责的所有实验进程。状态：`USER_STOPPED`。

## 已执行

- 2026-09-09 13:51 CST 核对 N16R4 当前 Slurm 队列及作业目录，本任务运行中和排队中的训练/评测均为 0；未对已结束的历史 job 再发取消命令。
- 13:52:44 CST 向已核验身份的每分钟监督器 PID `1914589` 发送 `SIGTERM`；5 秒后进程消失，13:53:54 CST 再查无监督进程重启，最后轮询回执停在 13:52:14 CST。
- 远端账户没有 crontab；本地 13:54 CST 未发现本任务匹配的训练/提交进程或 OpenTAD/DUCA Windows 计划任务。
- 13:57 CST 扩大远端进程关键词检查后，仅匹配 GeoSparse 独立项目的两个队列进程 PID361594/4169978，均未操作；没有本任务遗留的路线监督进程。
- 当前其他项目的 `1278774`、`1281614_0-3`、`1281582`、`1281445` 均未操作；历史 BAFDR `1267920/1267921` 保持只读。
- 所有日志、checkpoint、评测收据和原有性能结果保留；没有启动新的训练、GPU 诊断、PRECHECK 或评测。

## 本地心跳限制

`opentad-c3-duca` 的本地 `automation.toml` 仍为 `ACTIVE`。当前工具清单没有 `automation_update`，不能声称已经停用应用级定时任务。没有绕过管理接口修改该配置。

已将停止命令置于本任务 `AGENTS.md` 顶部。若旧心跳再次触发，不得连接集群、恢复监督器或重提实验；应在管理工具可用时停用心跳。用户可在应用的定时任务界面暂停“OpenTAD C3 DUCA 实验监督”。

本次停止覆盖本任务的六路线实验和监督，不改变其他独立任务的权限和状态。仅在用户此后明确授权恢复时才能恢复实验。原结果、未解决问题和停止前计划继续保留为历史记录，不再自动执行。

目录停止状态回归及仓库 focused tests：29 passed。新增回归确认停止后目录生成不轮询远端、不改写成绩或历史训练状态、不改变其他任务的下一步计划。

机器可读证据：[49_USER_STOP_20260909.json](49_USER_STOP_20260909.json)。最后一次完整性能核验仍为 [48_HEARTBEAT_EVIDENCE_20260909_1335.json](48_HEARTBEAT_EVIDENCE_20260909_1335.json)，本次不伪装为新一轮性能评测。
