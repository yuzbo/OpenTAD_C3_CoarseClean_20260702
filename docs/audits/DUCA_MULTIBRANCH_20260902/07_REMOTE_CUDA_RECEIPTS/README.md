# Remote CUDA receipts

This directory is intentionally empty at audit time. A receipt is admissible only when it records the exact source SHA, clean-tree status, host/software identity, visible devices/ranks, checkpoint hashes, command, exit status, and complete output. Windows skipped tests and scheduler metadata are not substitutes.
