# 04:15 实验性能检查

用户请求：检查实验性能。实际远端检查时间：2026-09-09 04:15:06-04:16:38 CST。
这是一次交互请求检查，不补记为未执行的历史心跳。较36号记录，没有新的性能改善或正式重提。

## 最新完整训练结果

五项均为 COMPLETED(0:0)，每项完成第5、10、...、60轮共12次完整THUMOS14测试，合计60次。
每次收据均记录211个测试视频、792个窗口；60份周期、5份best及5份协议收据自哈希有效。
best与未四舍五入Avg-mAP的最大值及最早同分轮次一致。

| 实验（外部可读名称） | 训练Job | 最佳Avg-mAP (完成轮数) | 第60轮Avg-mAP | 第60轮mAP@0.7 |
|---|---|---:|---:|---:|
| 均匀抽取 384 帧匹配对照 | 1280127 | 63.9646% (50) | 63.8596% | 42.0630% |
| 动态选帧、关闭四相分配 | 1280128 | 64.2632% (60) | 64.2632% | 43.0885% |
| 四相 128+64+64+128 选帧 | 1280129 | 57.5081% (55) | 57.4857% | 34.2285% |
| 完整 Transformer 匹配对照 | 1280118 | 62.5093% (35) | 61.7587% | 42.7085% |
| Transformer 内部低秩残差近似开启 | 1280117 | 55.3617% (40) | 54.5976% | 33.4394% |

H65训练源码为629162cdddbe490d896899a067e9803e237df467，ET为9a346f0d71e8870ad499bb0a58b9f1824b8904c0；
本轮远端HEAD仍精确匹配且工作树干净。全部stdout以Training Over结束，stderr尾部为正常推理进度。

## 能下什么结论

- 四相开启比关闭：最佳低6.7550个百分点，终轮低6.7774个百分点；终轮高IoU的mAP@0.7也低8.8600点。
- 低秩近似开启比关闭：最佳低7.1476个百分点，终轮低7.1612个百分点；终轮mAP@0.7低9.2691点。
- 已经完整训练，不能把这些负结果归因于训练尚未完成或GPU未分配。
- 这些单种子配对结果支持“当前实现及配方下模块未带来收益”，不单凭分数断言整个路线理论错误，也不据训练正常结束宣称实现一定正确。
- 当前本任务这些修正版结果仍没有超过65.6%；最高的64.2720%来自CT均匀对照。不同配方之间不据微小分差作胜负推断。

## 权重、协议与结果边界

五份epoch59及best文件的路径、大小和mtime与36号一致，本次不重复加载约0.6GB的未变权重；
复用34/35号ET和36号H65的实际权重、有限性与更新数核验。
H65全局optimizer/scheduler/EMA/DUCA schedule均6000；两项动态选帧中少数参数的Adam step低于6000，
其模块归属与条件参与语义仍待核对，不把它虚写成每个参数均6000，也不据此伪造全局预算失败。
ET没有独立存储EMA更新计数；该次数依赖FP32成功更新控制流证据，不冒称读取到了独立计数。

五项新SHA的独立终态评测收据仍未补齐。本表是训练过程中官方全测试EMA评测，
不是重新加载终态后独立封存的最终性能。最佳与终轮均保留，不能只挑好看的分数。
按用户授权采用TEST_GUIDED_EXPLORATORY_EVAL5及测试集选模；不能称未见测试泛化或与原论文公平可比。
本次没有新增超参数尝试，允许测试集调参不等于已经调参。

官方原始AdaTAD仍须单独完成配方对齐和独立复评证据；不把本轮768帧Transformer对照、
本轮均匀384帧对照或修改后的REF-D768 67.58%冒充原始官方模型。

## 其余路线

- 连续时间几何路线CT-DP：fe1c53db训练、07383274独立评测。均匀G0 64.2720%，动态双相G1 61.6740%；两份原收据自哈希有效、epoch59、6000更新。此对照不是完整B-AMoD矩阵。
- 分块多分辨率路由与蒸馏BAFDR：710ce8a6的D160/G96/LATE/NOKD/U16五份终态仍在，但新metrics为0、FULL仍只有PRECHECK。旧更新数违规分数不能迁移为修复版结果；b142bffa后继padding修复仍缺生产screen/eval5。
- 预选阶段证据补漏Evidence-Recovery：ce767b4d旧A1为51.5968%。本轮使用原收据真实键evaluation_sha256校验PASS；F/A6独立metrics仍缺。旧分数不代表79aea08a接线修复后的效果，utility/真实two-view/cycle仍未完整落地。
- 系统因素消融DUCA-Unified：后继0fcef0bf时间轴接线修复不等于Taylor P0/P1、合法one-swap或H65 retention/transition完成，仍无正式结果。

## 未解决失败与资源

已再次读取相关历史90轮H65作业1280314/1280316的stdout/stderr，仍FAILED(1:0)，
在stage2 epoch16的AMP replay后触发p_action calibration check received non-finite values；
直接抛出点为opentad/models/duca/acquisition.py:2042，最早数值污染来源尚未定位。
原任务“查找90轮H65实验配置”负责实施；本监督保留35号完整证据与失败记录，未接管、删除校验或盲目重提。
历史BAFDR 1267920/1267921未取消、修改或重提。

04:16公共可见未分配GPU为51/200；账户1 RUNNING、3 PENDING，均非本次五项已完成训练；
实验盘121.60GiB可用。公共余量不保证个人即时额度。
本轮只读检查远端并更新记录，没有模型修改、Pro咨询、新Slurm提交或取消。
后续实际缺项是独立终态评测、负结果的机制定位及未完成路线实现，而非等待这五项继续训练。

结构化证据：[37_PERFORMANCE_CHECK_20260909_0415.json](37_PERFORMANCE_CHECK_20260909_0415.json)。
