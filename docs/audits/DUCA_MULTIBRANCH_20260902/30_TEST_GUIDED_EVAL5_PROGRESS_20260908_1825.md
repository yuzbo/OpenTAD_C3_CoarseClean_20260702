# 测试集选模协议：首批真实曲线与最佳 EMA 核查

核查窗口：2026-09-08 18:16:11 至 18:25:20 CST。以下是实际读取远端结果的时间，不补记未执行检查。

## 新协议已生效

用户授权按测试集选择最佳 checkpoint 和调参。当前五项新实验均为 `TEST_GUIDED_EXPLORATORY_EVAL5`：
完成第 5、10、...、60 轮后评测完整 THUMOS14 测试集，按未四舍五入 Avg-mAP 选 EMA，同分取较早轮次。
测试集参与选模/调参，不能称独立未见测试泛化或与原论文公平可比；终态 epoch-59 EMA 和 6000 次成功更新仍单列。
本次只收集结果，没有新超参数尝试，没有取消或重新提交训练。既有失败修复规则不变。

## 正在运行的五项实验

下表 Avg-mAP 均由官方评估器对 211 个测试视频、792 个窗口计算。仍在训练，所有数值都是中期结果。

| 实验完整名称 | Slurm Job | 第 5 轮 | 第 10 轮 | 当前最佳 EMA | 该轮成功更新 |
|---|---|---:|---:|---|---:|
| Transformer 低秩残差近似开启：修复锚点后重训 | 1280117 | 5.7073% | 32.0496% | 第 10 轮 | 1000 |
| 完整 Transformer 对照：相同双 GPU 与 FP32 训练 | 1280118 | 4.2952% | 33.7828% | 第 10 轮 | 1000 |
| 均匀抽取 384 帧：整窗时间适配器匹配对照 | 1280127 | 5.2404% | 尚未返回 | 第 5 轮 | 500 |
| 动态选帧但关闭四相分配：整窗时间适配器对照 | 1280128 | 5.4013% | 尚未返回 | 第 5 轮 | 500 |
| 四相预算 128+64+64+128：整窗时间适配器实验 | 1280129 | 4.9665% | 尚未返回 | 第 5 轮 | 500 |

18:25 Slurm 五项均为 RUNNING。Transformer 两组从 17:21:31 起运行约 1 小时 4 分钟；
H65 三组从 17:26:01 起运行约 59 分钟。没有任何新 epoch-59 checkpoint 或终态成绩。

实际以 CPU 加载五份 `test_guided/best_test.pth`：
- 全部包含 `state_dict_ema`，EMA 张量无 NaN/Inf。
- 内部 source SHA、seed、epoch 和指标均与对应不可变轮次 metrics.json 一致。
- 七份轮次收据和五份当前最佳收据的现有自哈希均一致。
- ET-TRC 当前最佳已由第 5 轮自动更新为第 10 轮，不是只写了配置但未执行选择。
- H65 第 5 轮审计中 optimizer/scheduler/EMA/DUCA schedule 均为 500，三组初始 AMP skipped attempts 为 2/3/2，已重放；没有 replay exhaustion。

第 5 轮分数很低，但 ET-TRC 两组第 10 轮均明显上升，不能把第 5 轮当最终失败。
这也不是已经证明方法有效：第 10 轮近似开启组仍比完整对照低约 1.73 个百分点，需继续观察后续曲线。
本轮没有为追分改变参数、选择性省略低分或重启健康训练。

## 其他四条路线

- CT-DP 物理时间嵌入：修复版 G0/G1 训练和独立评测仍全部完成，终态 Avg-mAP 为 64.2720%/61.6740%。
  沿用此前实际 6000 次更新和收据验证，不重复准入/训练。几何组没有启用 B-AMoD；后续新矩阵尚需接入 eval5。
- BAFDR 分块多分辨率路由与蒸馏：teacher/G96/U16/LATE/NOKD 的五个终态文件保留，
  新版尚无独立 metrics 文件；FULL 只有 PRECHECK 完成。真实 wrapper 的 padding、终态 screen 和专用训练器 eval5 尚未完成。
- Evidence-Recovery 预选阶段证据补漏：旧修复 A1/A6/F 终态保留；A1 独立 Avg-mAP 仍为 51.5968%，
  F/A6 尚缺独立收据。新 refiner 初始化修正不代表 utility 学习、真实 robust/cycle 已落地。
- DUCA-Unified 系统消融：仍缺真实 Taylor P0/P1、合法 one-swap、H65 retention/transition，无正式结果。
  这是实现阻塞，不是只差 GPU。

## 资源与监督

18:20:44 可见可调度节点 GPU 分配为 200/200，未分配 0；账户 8 RUNNING、2 PENDING，
两项等待均为 JobHeldUser，其他任务不操作。磁盘剩余 163.2 GiB。
分钟轮询回执 18:20:40 生成，距采集 4 秒，dispatcher 仍为 plan/BLOCKED、entries 为空；
不能称其会自动部署当前修复队列。

本次没有新提交、取消、模型修改或 Pro 咨询，历史 BAFDR 1267920/1267921 未操作。
主仓库 AGENTS 中旧的禁测试集选模条款已按用户新授权作范围明确的修订，保留原有其他路线规则。
每半小时监督继续收集完整曲线、最佳与终态结果及所有调参尝试。

结构化证据：[30_TEST_GUIDED_EVAL5_PROGRESS_20260908_1825.json](30_TEST_GUIDED_EVAL5_PROGRESS_20260908_1825.json)。
部署身份与路径沿用 [29_TEST_GUIDED_EVAL5_DEPLOYMENT_20260908.md](29_TEST_GUIDED_EVAL5_DEPLOYMENT_20260908.md)。

