# DUCA 代码库存边界（2026-08-28）

本分支用于让科学负责人完整查看当前代码与研究记忆，并首先决定科学路线和代码整理方式。它不是新的实验候选，也没有通过运行前检查。

## 组成

分支从 PJST-D1 只读终态评估提交 `7bd120f0d342bf175c97c365fba7cbd359df055e` 建立，因此保留 H65/PJST-D1 的模型、配置、测试和终态评估入口。在此基础上，同步项目根当前工作区中的：

- `opentad/` 模型、数据变换、检测器与评估代码；
- `configs/` 实验配置；
- `tools/` 训练、评估、分析与运行前检查工具；
- `scripts/` 本地与 N16R4/Slurm 启动器；
- `tests/` 聚焦测试；
- `docs/` 中与 DUCA 设计和审查相关的文档；
- `research-wiki/`、`PAPER_PROGRESS.md`、`RTK.md` 与 `AGENTS.md`。

没有同步数据集、检查点、训练输出、浏览器状态、凭据、临时目录或服务器日志。

## 重要解释

项目根工作区长期承担多条路线的材料整合，当前根 `HEAD` 为 `a6bdc084cc145c80b6b2c68d0a38f0deea3e8518`，属于 SparseHead 文档历史，不能代表 DUCA。根工作区中的代码又包含 DUCA、物理时间诊断及并行路线的未归档改动。因此，本分支只具有“代码库存”的意义：它让 Pro 看见现有实现表面和重叠关系，但不能从分支整体状态推断某个实验已经正确实现或得到结果。

存在同路径冲突的历史实现没有被机械合并。它们仍以各自的干净提交为权威：

- H65 30+60：`04c35a3b76897e6c1569eeede41ed3aecaf7f854`；
- H65 学习率诊断：`ae3642a138c5b2e1ac2daad75a6d43d17cdb6c2f`；
- First-Mixing SingleClock：`b2ccfccab5b4912b59954afcc9b0364955327f7c`；
- PJST-D1 训练：`c73e8418de31cdcb2a445ff58a1e33ab9ab6a508`；
- PJST-D1 终态评估：`7bd120f0d342bf175c97c365fba7cbd359df055e`；
- UVT：`df544c78ce515d925dc7019f106fce09a53c09f8`；
- Fovea/Query-Bridge：`4ae5067100c4490c7110c00a1ad406230ba603cd`。

## 第一个科学任务

Pro 应先依据固定提交、当前库存与实验结果完成两件事：

1. 独立判断当前值得继续的论文问题和主路线，不把现有分支名称或 Codex 的候选列表当作结论；
2. 指定一个最小、可审计的权威代码主线：哪些实现必须保留或复用，哪些只保留为历史对照，哪些混杂表面应移出当前候选，以及下一次实验只允许修改哪些文件和科学变量。

在该判断完成前，本库存分支不得直接用于正式训练或论文结果归因。
