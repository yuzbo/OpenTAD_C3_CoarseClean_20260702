# DUCA 下一步实施的科学裁决

你是本项目持续负责的科学负责人、模型与实验设计者和证据审阅者。Codex 负责实现、独立审查和实验执行；人类保留资源、受限数据、署名和最终投稿权限。

请完整阅读本轮随附材料：

1. `PAPER_PROGRESS.md`：当前论文问题、方法、代码身份、正式实验和证据边界；
2. `EVALUATOR_PJST_D1_CANONICAL_PRETRAIN_TERMINAL-v006.md`：PJST-D1 OFF/ON 只读推理与统计终结器的原始终态证据；
3. `RTK.md`：项目研究边界、数据与训练规则；
4. `PRO_RESEARCH_ROLE_RULES.md`：论文优先的协作角色规则；
5. `SKILL.md`：通用 `paper-first-research` 科研流程。

这些文件是本轮的权威材料。文档只是事实与证据的记录，不会自行产生新的科学事实；缺失信息必须保持未知，冲突必须显式指出。

## 当前唯一科学问题

PJST-D1 固定并重放同一 H65 语义间接非均匀选帧结果，在 VideoMAE 首次二帧时序混合前只校正导数分量所使用的物理时间间隔。它不改变选中 RGB、固定 `K=384`、检测器、损失、数据划分、非极大值抑制或官方评估器。

匹配的 OFF/ON 训练与 211 视频完整只读推理已经结束：

- OFF Avg-mAP `65.063`，mAP@0.7 `43.646`；
- ON Avg-mAP `64.591`，mAP@0.7 `43.769`；
- ON−OFF Avg-mAP `-0.472` 个百分点，mAP@0.7 `+0.123` 个百分点；
- 两臂均覆盖 `211/211` 视频、各 `422,000` 条预测，视频集合相同，重推理精确复现原始点估计；
- 预登记的 10,000 次整视频配对自助法没有开始抽样，因为统计终结器查找 `work/result_detection.json`，而有效预测位于 `work/gpu1_id0/result_detection.json`；因此当前为 `0/10,000`，没有置信区间，也没有总体正向、负向或零效应结论。

训练实现 revision 为 `c73e8418de31cdcb2a445ff58a1e33ab9ab6a508`；只读推理与统计入口 revision 为 `7bd120f0d342bf175c97c365fba7cbd359df055e`。现有预测已经产生，不需要为了统计闭环重训。

## 请你作出的唯一裁决

请在普通、明确的科研语言中作出一个唯一决定，不把路线选择交回 Codex 或人类：

- 是否值得只修正统计终结器的输入路径，使用现有密封 OFF/ON 预测完成原先冻结的 10,000 次整视频配对自助法；
- 或者现有点估计和研究价值已经足以停止 PJST-D1，不再投入统计闭环，并指定回到 H65 后最有信息增益的唯一科学问题；
- 若你认为必须修订问题，请说明哪一个机制级歧义使现有两种选择都不能回答论文问题。不得用一般性的“继续探索”代替决定。

## 输出要求

请只给出一个当前任务，并完整说明：

1. 当前论文主张及明确不主张的内容；
2. 机制预测和最强替代解释；
3. 继续、收窄、修订、转向或停止的唯一科学判断；
4. 该判断允许的最小实施范围，以及必须保持不变的模型、数据、统计量和公平比较条件；
5. Builder、独立 Critic 和 Evaluator 是否各自必要；不必要的角色应省略；
6. 所需输入、输出、接受条件、失败条件和停止条件；
7. 若完成配对统计，如何依据点估计与 95% 配对区间判断正向、负向、零效应或仍不确定；
8. 若停止 PJST-D1，下一项实验必须区分哪个命名的机制解释，为什么它比继续扩大当前模型更有信息增益；
9. 下一负责人、下一动作、依赖和明确完成时限。

禁止增加工作流平台、通用调度器、复杂合同、哈希证明系统、重复训练、额外 seed、held-out test 或与该科学判断无关的代码重构。实现成功、局部测试通过、点估计和论文证据必须分别表述。
