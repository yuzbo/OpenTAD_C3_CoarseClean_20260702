# DUCA 科研连续性、流程优化与唯一下一任务问询

**Nonce：`DUCA-RESEARCH-CONTINUITY-NEXT-TASK-v001-20260828`**

## 一、你的身份与本轮责任

你是本项目持续任职的**首席科学负责人、整体科研流程设计者与维护者、模型和实验设计者、证据审阅者以及任务中枢**。你负责决定科学问题、创新机制、可证伪预测、研究路线、实验优先级、结果解释、论文主张边界，以及如何用最轻量的流程让研究连续推进。

Codex 是你的执行与证据反馈系统。它负责在你冻结的科学任务内完成最小代码实现、安排独立代码审查、运行前核验、正式实验、原始证据整理和研究记忆更新。Codex 可以主动向你返回新证据或询问下一步，但不能预设你的路线选择。人类保留法律意义上的论文作者身份、资源与凭据授权以及最终投稿决定。

本轮不能只做一次 detached review。你必须同时完成两件事：

1. 根据完整代码与证据，独立决定 DUCA 当前唯一科学任务；
2. 诊断过去一天反复出现的路线中断、实现中断、证据链中断和浏览器讨论中断，并给出可直接写入项目规则的轻量改进，使 Codex 能持续完成一整轮“实现—独立审查—正式实验—证据返回”，而不是每遇到普通故障就停止或重新讨论。

不要让 Codex 或人类在你列出的多个候选中替你选线。你的输出必须包含一个明确的 `CONTINUE`、`REVISE`、`PIVOT` 或 `STOP`，以及一个且只有一个当前任务。

## 二、当前权威项目与代码

当前唯一权威 ChatGPT Project：

`https://chatgpt.com/g/g-p-6a91061f789881918ccd8357ca3d6c92/project?tab=chats`

GitHub repository：

`https://github.com/yuzbo/OpenTAD_C3_CoarseClean_20260702`

当前完整代码与 Wiki 库存：

- branch：`codex/duca-research-sync-20260828`
- code inventory revision：`5136011ed57df8a639427a633a488a592ba95924`
- 上一版完整路线/代码整理材料：`29eb779824d4f8146d557d1225c7634a0acba85c`
- 上一版完整 prompt：`https://github.com/yuzbo/OpenTAD_C3_CoarseClean_20260702/blob/29eb779824d4f8146d557d1225c7634a0acba85c/docs/pro-packets/DUCA_PROGRESS_SYNC_AND_ROUTE_DECISION-v001/00_PROMPT.md`

请把 GitHub 固定提交视为代码真值。库存分支用于让全部历史和并行实现可见，不自动等同于一个干净、可运行、可归因的候选。需要判断实现时，应直接读取相关固定提交中的真实代码、配置、启动器和测试，而不是只相信摘要。

建议先读：

- `research-wiki/DUCA_CURRENT_RESEARCH_STATE-2026-08-28.md`
- `PAPER_PROGRESS.md`
- `research-wiki/DUCA_WIKI_MEMORY_AUDIT-2026-08-17.md`
- `research-wiki/decision_history.md`
- `research-wiki/anti_repetition.md`
- `research-wiki/duca_model_version_registry.md`
- `research-wiki/PRO_RESEARCH_ROLE_RULES.md`
- `RTK.md`

需要直接核验的主要实验身份：

- H65 30+60：`04c35a3b76897e6c1569eeede41ed3aecaf7f854`
- H65 第二阶段学习率诊断：`ae3642a138c5b2e1ac2daad75a6d43d17cdb6c2f`
- First-Mixing SingleClock：`b2ccfccab5b4912b59954afcc9b0364955327f7c`
- PJST-D1 匹配训练：`c73e8418de31cdcb2a445ff58a1e33ab9ab6a508`
- PJST-D1 终态只读评估：`7bd120f0d342bf175c97c365fba7cbd359df055e`
- UVT：`df544c78ce515d925dc7019f106fce09a53c09f8`
- Fovea/Query-Bridge：`4ae5067100c4490c7110c00a1ad406230ba603cd`

这个新会话不依赖旧对话记忆。下面的事实账本与 GitHub 材料共同构成本轮上下文。

## 三、2026-08-27 12:00 至 2026-08-28 12:00 的真实工作账本（北京时间）

### 1. 完成 PJST-D1 结果前的科学问询与最小修复

- 向 Pro 提交 PJST-D1 训练后根因和下一任务材料。Pro 给出 `REVISE`：当前 ON−OFF 平均检测精度点差没有正向支持，但在预登记的整视频配对区间产生前不能称为正式负结果；先做一次且仅一次不改变科学语义的绝对预训练权重路径修复，再复用既有 OFF/ON 最终指数移动平均权重做官方 211 视频只读推理和 10,000 次配对自助法，不重训、不改模型、不改数据、不改评估器。
- Builder 在父提交 `4204937a933c7a48854b623efefc7fd662e98805` 上完成最小启动器修复，形成干净提交 `7bd120f0d342bf175c97c365fba7cbd359df055e`。改动只涉及既有终态评估启动器、聚焦测试和角色回执，没有模型、选择器、数据、NMS、评估器或统计量漂移。
- 独立 Critic 对该固定快照完成只读审查并给出静态通过，因此没有进入新的 Builder 修复循环。

### 2. 执行 OFF/ON 官方只读推理，但统计终结器中断

- Evaluator 在 N16R4 上完成 OFF job `1257897` 与 ON job `1257898`，二者均正常退出。
- 两臂均覆盖官方 validation 的 `211/211` 个视频，每臂产生 `422,000` 条预测，视频 ID 集合一致；使用 epoch-59/第 60 轮的指数移动平均权重，点估计与既有记录精确复现。
- OFF Avg-mAP=`65.063283`，ON Avg-mAP=`64.590802`，ON−OFF Avg=`-0.47248126` 个百分点。
- ON−OFF 在 tIoU 0.3/0.4/0.5/0.6/0.7 的差分别为 `-0.79522098/-1.25244448/-0.14698444/-0.29049525/+0.12273884` 个百分点。
- 统计终结器 job `1257899` 在 bootstrap 前失败：启动 argv 指向扁平路径 `work/result_detection.json`，真实预测位于 `work/gpu1_id0/result_detection.json`。因此 `0/16` 个分片、`0/10000` 次配对重复，完全没有置信区间。
- 这意味着：点估计没有正向支持，但不能据此宣布总体效应显著为负；高 tIoU 的小幅正差也不能写成收益。失败是证据生成路径错误，不是模型效能结论。上一执行链按当时边界关闭，没有自动重试。

### 3. 整理全部代码、实验与研究记忆并同步 GitHub

- 从 PJST-D1 终态评估身份出发建立专用同步分支 `codex/duca-research-sync-20260828`，把当前 `opentad/`、`configs/`、`tools/`、`scripts/`、聚焦测试、DUCA Wiki 和论文进展同步为代码库存提交 `5136011...`；该提交包含 297 个文件，仅表示当前可见库存，不声称所有共享表面能够机械合并或形成干净候选。
- 新建并提交完整的“路线决策与代码整理”Pro 材料 `29eb779...`，其中逐项列出权威 GitHub 链接、固定实验提交、历史正负结果、未知项、代码整理问题和唯一任务要求。
- 本地另行整理了一个待选 Source 目录 `docs/pro-packets/DUCA_ROUTE_CODE_ORGANIZATION_UPLOAD-v001/`，包含 prompt、当前研究状态、代码库存边界、Evaluator 原始证据、项目规则、角色规则、通用 paper-first skill 与模型登记。该目录只是本地准备材料，未宣称已经上传到 Project。
- 将论文优先、Pro 科学所有权、科研语言清晰和上下文忠实原则写入项目规则；保持 `PAPER_PROGRESS.md` 对外只陈述科学事实，不把浏览器、队列和普通工程故障冒充论文进展。

### 4. 尝试在上一 DUCA Project 发起路线/代码整理 Pro 讨论，但没有得到可用科学裁决

- 第一次 Oracle 预提交检查遇到登录页，未提交提示词，科学尝试次数为零。
- 人类重新登录后，第二次 invocation 在上一 DUCA Project 创建新会话并提交了完整 prompt，模型选择为最高可验证 Pro 路线；但结果回取只保存到“Pro 正在思考”的前言，随后超时，没有得到 `CONTINUE/REVISE/PIVOT/STOP`、代码主线或当前任务。
- 因终态内容不完整，该回复没有科学、实现或实验授权。没有把不完整文本当作 Pro 决策，也没有基于它启动新训练。
- 多次失败暴露出流程问题：浏览器 profile 被不同任务长期占用、运行端点和标签页状态漂移、Oracle 长思考与本地回取生命周期脱节、过度依赖短时租约或一次性前台进程、确定性路径错误被提升为整轮终止，以及科学任务在 Builder/Critic/Evaluator 之间缺少“普通修复后继续到真实证据”的稳定默认语义。

### 5. 截止 2026-08-28 12:00 的状态

- 没有正在运行的 DUCA 训练或正式评测作业。
- 没有新的动态预算效能或端到端成本证据。
- 当前最完整的可接受模型结果仍是：共享官方 AdaTAD Avg-mAP `68.73`；H65 30+60 Avg-mAP 约 `65.13`；PJST-D1 的 OFF/ON 点估计已复现但配对总体区间缺失；UVT 与 Fovea/Query-Bridge 的负向单种子结果只能作为设计诊断。
- 当前真正的首要事项不是再发明一个并列机制，而是由你完成路线裁决、指定权威代码主线，并把下一轮执行设计成会抵达一个真实、可准入的科学返回。

## 四、当前科学边界

DUCA 的长期问题是：在离线时序动作检测中，能否用低成本侦察模型预测逐时刻二元动作性和动作起止边界重要性，再由确定性规则间接选择非均匀原始帧，并根据逐视频或逐窗口语义证据分配动态预算，从而真实减少 VideoMAE 高分辨率计算，同时保护高时间交并比下的边界定位。

人类已明确：

1. 学习任务是动作性与边界语义预测；直接预测帧索引只能作消融。
2. 动态预算是长期论文核心；固定 K 只能作归因、对照或回退。
3. 非连续选帧必须让原始时间戳或物理坐标贯穿重型编码和 NMS 前解码，不能把稀疏序号伪装成均匀时间。
4. 未修改官方 AdaTAD 复现全局只做一次，DUCA 不重复训练。
5. 正式实验使用完整 THUMOS14 官方划分和评估器，公平匹配检测器、损失、NMS、成功更新数、最终权重规则与实际端到端成本。
6. 完整训练至少每 5 个 epoch 保存可恢复检查点；不能按中间验证事后挑最好。
7. 局部测试、代码审查、作业启动成功和基础设施回执不是模型有效性证据。

这些是人类定义的问题与公平性边界，不是预设路线。你可以依据证据 `CONTINUE`、`REVISE`、`PIVOT` 或 `STOP`，但必须说明新判断如何处理 H65、PJST-D1、UVT、Fovea/Query-Bridge 和动态预算的正负证据。

## 五、请你首先诊断并重写科研连续性流程

请以整体科研流程的设计者与维护者身份，直接回答并落实以下问题。

### 1. 如何避免路线和流程频繁中断？

请区分至少四类事件，并明确每类事件发生后是否继续、修复、返回你裁决或停止：

- 真实科学反证、机制歧义或公平性/泄漏问题；
- claim-preserving 的确定性代码、路径、配置或环境错误；
- 外部浏览器、模型生成、远端队列或凭据等 transport/infrastructure 故障；
- 只有点估计但统计或证据准入不完整的情况。

目前最突出的问题是：一个不改变模型或科学合同的路径错误，也会关闭整个 Evaluator 链；一个长时间 Pro 思考也会因本地回取超时丢失；随后又发起新的流程讨论，导致科学问题长期没有到达真实裁决。请给出更好的默认恢复语义，但不要取消数据合法性、公平比较、独立审查和证据边界。

### 2. 如何让 Codex 持续执行到一轮有效返回？

请定义“完成一轮”的最小含义，并给出 Codex 可直接遵守的连续执行规则。例如，一轮应至少从你的冻结任务开始，经过最小实现、一次独立审查、运行前核验、一个真正会改变科学判断的官方实验或统计分析、结果归因，再把完整证据返回给你。

重点说明：

- 哪些普通实现缺陷允许 Codex 在不重新问你的情况下做一次最小修复并继续；
- 哪些缺陷必须停止并返回你；
- Critic 和 Evaluator 如何报告，使“needs attention”不是默认终点；
- Pro 深度思考超过一小时、Oracle 前台超时或浏览器端口漂移时，如何保存同一 conversation/session 并继续回取，而不是重复提交；
- 如何防止浏览器锁永久卡死，同时遵守“当前 profile61 已重新登录，绝对不得关闭、重启或替换当前浏览器/网页”的约束；
- 如何用事件驱动和耐久状态保持 Codex 长线运行，而不建立数据库、daemon、dashboard 或通用工作流平台；
- 如何避免固定审查次数、角色文档和合同代码变成实验前的流程负担。

### 3. 给出整体流程的最小规则改写

请审阅仓库中的 `RTK.md`、`research-wiki/PRO_RESEARCH_ROLE_RULES.md` 和通用 `paper-first-research` skill。输出可直接机械写入的最小文本改动：

- 哪些现有规则应删除、合并或软化；
- 哪些持续执行、恢复、科学返回和 Pro 再裁决规则必须新增；
- 每条修改解决哪一种已发生的失败；
- 如何保持“论文与模型创新优先、Pro 科学所有权、Codex 持续执行、独立核验、不过度工程”同等成立。

不要设计新的调度平台、状态机产品、复杂 schema、哈希证明系统或多层审批。若两三条自然语言规则足以解决问题，就不要写二十条。

## 六、请独立决定 DUCA 当前路线与唯一下一任务

在流程诊断后，请完成此前没有得到终态回复的科学与代码裁决：

1. 用一个明确词开头：`CONTINUE`、`REVISE`、`PIVOT` 或 `STOP`。
2. 指定当前最值得发表的科学问题、机制、令人意外的预测、反解释和最便宜的决定性实验。
3. 指定一个权威干净基座提交，以及必须保留/复用、仅作历史对照、应从主候选移除的代码表面。
4. 判断是否有必要补齐 PJST-D1 的 10,000 次配对区间。不要因为它最短而机械选择，也不要因负点估计而跳过有决策价值的证据；由你判断它是否会改变路线。
5. 只下达一个当前任务。任务要明确 Builder、独立 Critic、Evaluator 的工作，允许修改的文件/符号，禁止改变的科学合同，正式实验、指标、停止规则、原始证据返回和北京时间绝对截止时间。
6. 给出这轮失败时的根因分析分叉：哪些结果支持机制，哪些反驳机制，哪些只说明实现/统计未决；每种结果后 Codex 应如何继续并在何时返回你。

不要机械重复已经完成多次的 dense、uniform、random 基础实验。若当前唯一任务确实需要某个旧基线，必须先说明已有固定提交为何不能提供公平身份。

## 七、输出要求

请使用熟悉机器学习论文的研究者首次阅读即可理解的中文，不创造内部缩写或私人状态语言。按以下结构返回：

1. **唯一科学裁决**；
2. **过去 24 小时事实与证据核验**；
3. **流程中断根因**；
4. **轻量科研 Pipeline 与完成一轮的定义**；
5. **对项目规则和通用 Skill 的最小可写入改动**；
6. **权威代码主线与整理决定**；
7. **唯一当前任务单、决定性实验与绝对截止时间**；
8. **结果返回后你的下一次裁决条件**；
9. **当前可写入论文与仍不可声称的边界**。

必须由你作出选择和任务下达，不能让 Codex 或人类从多个并列选项中再选一次。流程建议必须帮助 Codex 抵达真实实验和有效返回，而不是扩大工程治理。
