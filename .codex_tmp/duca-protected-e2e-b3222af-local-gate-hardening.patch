diff --git a/configs/adatad/thumos/duca_protected_e2e_fixed384_official60.py b/configs/adatad/thumos/duca_protected_e2e_fixed384_official60.py
index 1750807..ad681d3 100644
--- a/configs/adatad/thumos/duca_protected_e2e_fixed384_official60.py
+++ b/configs/adatad/thumos/duca_protected_e2e_fixed384_official60.py
@@ -58,6 +58,10 @@ duca_transition_only_contract = dict(
     detector_gradient_is_direct=True,
     detector_gradient_final_weight=0.25,
     detector_gradient_updates="transition_scorer_only",
+    adatad_pretrain_sha256="4b96b7f403f8ae0396437855b785af6a0064f11a9d76e2268e5a76a04e0de251",
+    official_asformer_source_normalized_lf_sha256=(
+        "e075ee4825a201cfe324d5fbfb1332c0800f532e85b9d3809f6ca5180381c600"
+    ),
     policy_hidden_gradient_scale=0.0,
     action_head_detector_gradient=False,
     asformer_trunk_detector_gradient=False,
diff --git a/scripts/run_duca_protected_e2e_gates_gpu1.sh b/scripts/run_duca_protected_e2e_gates_gpu1.sh
index bc1bacb..2f5acbe 100755
--- a/scripts/run_duca_protected_e2e_gates_gpu1.sh
+++ b/scripts/run_duca_protected_e2e_gates_gpu1.sh
@@ -21,6 +21,7 @@ CHECKPOINT_SHA256="${DUCA_PROTECTED_E2E_AUDIT_CHECKPOINT_SHA256:-}"
 CHECKPOINT_SOURCE_COMMIT="${DUCA_PROTECTED_E2E_AUDIT_CHECKPOINT_SOURCE_COMMIT:-}"
 CHECKPOINT_EVIDENCE="${DUCA_PROTECTED_E2E_AUDIT_CHECKPOINT_EVIDENCE:-}"
 CHECKPOINT_EVIDENCE_SHA256="${DUCA_PROTECTED_E2E_AUDIT_CHECKPOINT_EVIDENCE_SHA256:-}"
+ADATAD_PRETRAIN_SHA256="${DUCA_PROTECTED_E2E_ADATAD_PRETRAIN_SHA256:-}"
 
 [[ -n "${SLURM_JOB_ID:-}" ]] || fail "gates must run inside Slurm"
 [[ -n "${CUDA_VISIBLE_DEVICES:-}" ]] || fail "Slurm did not expose a logical GPU"
@@ -42,11 +43,14 @@ CHECKPOINT_EVIDENCE_SHA256="${DUCA_PROTECTED_E2E_AUDIT_CHECKPOINT_EVIDENCE_SHA25
 [[ "$(sha256sum "${CHECKPOINT_EVIDENCE}" | awk '{print $1}')" == "${CHECKPOINT_EVIDENCE_SHA256}" ]] \
   || fail "checkpoint evidence hash drift"
 [[ -f "${ADATAD_PRETRAIN_PATH}" ]] || fail "AdaTAD pretrain is missing"
+[[ "${ADATAD_PRETRAIN_SHA256}" =~ ^[0-9a-f]{64}$ ]] \
+  || fail "preregistered AdaTAD pretrain SHA256 is missing"
+[[ "$(sha256sum "${ADATAD_PRETRAIN_PATH}" | awk '{print $1}')" == "${ADATAD_PRETRAIN_SHA256}" ]] \
+  || fail "AdaTAD pretrain hash drift"
 [[ -f "${C3_OFFICIAL_ACTION_SEG_REPOS}/ASFormer/model.py" ]] \
   || fail "official ASFormer source is missing"
 
 mkdir -p "${GATE_ROOT}/contracts" "${GATE_ROOT}/tests" "${GATE_ROOT}/gradients" "${GATE_ROOT}/alignment"
-ADATAD_PRETRAIN_SHA256="$(sha256sum "${ADATAD_PRETRAIN_PATH}" | awk '{print $1}')"
 
 configs=(
   configs/adatad/thumos/duca_exact_uniform_fixed384_official60.py
diff --git a/tools/bata/run_duca_protected_e2e_exact_full_model_gate.py b/tools/bata/run_duca_protected_e2e_exact_full_model_gate.py
index 3990278..815f3c7 100644
--- a/tools/bata/run_duca_protected_e2e_exact_full_model_gate.py
+++ b/tools/bata/run_duca_protected_e2e_exact_full_model_gate.py
@@ -29,9 +29,19 @@ from opentad.cores.optimizer import (
     build_optimizer,
     prepare_optimizer_parameter_freezing,
 )
+from opentad.cores.scheduler import build_scheduler
+from opentad.cores.train_engine import (
+    _capture_custom_replay_state,
+    _capture_model_buffers,
+    _capture_rng_state,
+    _restore_custom_replay_state,
+    _restore_model_buffers,
+    _restore_rng_state,
+)
 from opentad.datasets import build_dataloader, build_dataset
 from opentad.models import build_detector
 from opentad.models.selectors.duca_online_frame_selector import _gather_time
+from opentad.utils import ModelEma
 from tools.bata.validate_duca_protected_e2e_official60 import validate_config
 
 
@@ -194,6 +204,161 @@ def _scaled_backward(
     return partition
 
 
+def _representative_update_parameters(model) -> dict[str, tuple[str, torch.nn.Parameter]]:
+    predicates = {
+        "detector_head": lambda name: name.startswith("rpn_head."),
+        "backbone_adapter": lambda name: name.startswith("backbone.")
+        and "adapter" in name,
+        "selector_scorer": lambda name: name.startswith(
+            "frame_selector.adapter.transition_scorer."
+        ),
+        "coarse_probe": lambda name: name.startswith(
+            "frame_selector.raw_actionness_source.probe_module."
+        ),
+    }
+    output = {}
+    for group, predicate in predicates.items():
+        candidates = [
+            (name, parameter)
+            for name, parameter in model.named_parameters()
+            if predicate(name)
+            and parameter.requires_grad
+            and parameter.grad is not None
+            and torch.isfinite(parameter.grad).all().item()
+            and float(parameter.grad.detach().abs().sum().item()) > 0.0
+        ]
+        _require(candidates, f"cost backward produced no update candidate for {group}")
+        output[group] = candidates[0]
+    return output
+
+
+def _run_real_cost_optimizer_update(
+    *,
+    ddp,
+    model,
+    optimizer,
+    scheduler,
+    model_ema,
+    batch: Mapping[str, Any],
+    clip_grad_norm: float,
+    max_retries: int,
+) -> dict[str, Any]:
+    rng_state = _capture_rng_state()
+    buffer_state = _capture_model_buffers(ddp)
+    custom_state = _capture_custom_replay_state(ddp)
+    scaler = torch.cuda.amp.GradScaler(enabled=True)
+    schedule_before = int(model.frame_selector._loss_weight_schedule_step.item())
+    scheduler_before = int(scheduler.last_epoch)
+    ema_root = getattr(model_ema.module, "module", model_ema.module)
+    for retry in range(int(max_retries) + 1):
+        if retry > 0:
+            _restore_rng_state(rng_state)
+            _restore_model_buffers(ddp, buffer_state)
+            _restore_custom_replay_state(ddp, custom_state)
+        optimizer.zero_grad(set_to_none=True)
+        with torch.autocast(
+            device_type="cuda",
+            dtype=torch.float16,
+            enabled=True,
+            cache_enabled=False,
+        ):
+            losses = ddp(
+                batch["inputs"],
+                batch["masks"],
+                batch["metas"],
+                gt_segments=batch["gt_segments"],
+                gt_labels=batch["gt_labels"],
+                return_loss=True,
+            )
+            cost = losses["cost"]
+        _require(cost.ndim == 0 and torch.isfinite(cost).item(), "real cost is non-finite")
+        scaler.scale(cost).backward()
+        scaler.unscale_(optimizer)
+        torch.nn.utils.clip_grad_norm_(model.parameters(), float(clip_grad_norm))
+        finite_gradients = all(
+            parameter.grad is None or torch.isfinite(parameter.grad).all().item()
+            for parameter in model.parameters()
+        )
+        representatives = (
+            _representative_update_parameters(model) if finite_gradients else {}
+        )
+        before = {
+            group: parameter.detach().clone()
+            for group, (_name, parameter) in representatives.items()
+        }
+        ema_before = {
+            group: ema_root.get_parameter(name).detach().clone()
+            for group, (name, _parameter) in representatives.items()
+        }
+        scale_before = float(scaler.get_scale())
+        scaler.step(optimizer)
+        scaler.update()
+        scale_after = float(scaler.get_scale())
+        optimizer_step_ran = scale_after >= scale_before
+        if not optimizer_step_ran:
+            continue
+        _require(finite_gradients, "GradScaler stepped despite non-finite gradients")
+        schedule_summary = model.after_optimizer_step()
+        scheduler.step()
+        model_ema.update(ddp)
+        schedule_after = int(model.frame_selector._loss_weight_schedule_step.item())
+        scheduler_after = int(scheduler.last_epoch)
+        _require(
+            schedule_after == schedule_before + 1,
+            "successful optimizer update did not advance selector schedule exactly once",
+        )
+        _require(
+            scheduler_after == scheduler_before + 1,
+            "successful optimizer update did not advance scheduler exactly once",
+        )
+        parameter_changes = {}
+        ema_updates = {}
+        for group, (name, parameter) in representatives.items():
+            changed = not torch.equal(before[group], parameter.detach())
+            _require(changed, f"successful cost update did not change {group}: {name}")
+            parameter_changes[group] = {
+                "name": name,
+                "changed": True,
+                "absolute_change_sum": float(
+                    (parameter.detach() - before[group]).abs().sum().item()
+                ),
+            }
+            ema_after = ema_root.get_parameter(name).detach()
+            expected_ema = (
+                float(model_ema.decay) * ema_before[group]
+                + (1.0 - float(model_ema.decay)) * parameter.detach()
+            )
+            _require(
+                torch.allclose(ema_after, expected_ema, rtol=1.0e-6, atol=1.0e-8),
+                f"EMA update differs from the formal update rule for {group}: {name}",
+            )
+            ema_updates[group] = {
+                "name": name,
+                "matches_formal_update_rule": True,
+                "absolute_change_sum": float(
+                    (ema_after - ema_before[group]).abs().sum().item()
+                ),
+            }
+        return {
+            "successful": True,
+            "retry_count": retry,
+            "amp_scale_before_successful_step": scale_before,
+            "amp_scale_after_successful_step": scale_after,
+            "cost": float(cost.detach().cpu().item()),
+            "clip_grad_norm": float(clip_grad_norm),
+            "schedule_step_before": schedule_before,
+            "schedule_step_after": schedule_after,
+            "schedule_summary": schedule_summary,
+            "scheduler_last_epoch_before": scheduler_before,
+            "scheduler_last_epoch_after": scheduler_after,
+            "parameter_changes": parameter_changes,
+            "ema_updates": ema_updates,
+        }
+    raise ExactGateFailure(
+        f"real cost optimizer update exhausted {max_retries} AMP retries"
+    )
+
+
 def run_gate(
     *,
     config_path: str,
@@ -220,6 +385,11 @@ def run_gate(
         _sha256(pretrain) == str(adatad_pretrain_sha256).lower(),
         "AdaTAD pretrain SHA256 mismatch",
     )
+    _require(
+        str(adatad_pretrain_sha256).lower()
+        == str(cfg.duca_transition_only_contract.adatad_pretrain_sha256),
+        "AdaTAD pretrain hash differs from the preregistered config contract",
+    )
     model_cfg = copy.deepcopy(cfg.model)
     model_cfg.backbone.custom.pretrain = str(pretrain)
     model = build_detector(model_cfg)
@@ -232,6 +402,14 @@ def run_gate(
         model.backbone.__class__.__name__ != "_ProofTemporalMeanBackbone",
         "proof backbone substitution is forbidden",
     )
+    asformer_source = (
+        model.frame_selector.raw_actionness_source.probe.official_source
+    )
+    _require(
+        asformer_source["source_normalized_lf_sha256"]
+        == cfg.duca_transition_only_contract.official_asformer_source_normalized_lf_sha256,
+        "official ASFormer source hash mismatch",
+    )
     logger = logging.getLogger("duca-protected-exact-gate")
     prepare_optimizer_parameter_freezing(copy.deepcopy(cfg.optimizer), model, logger)
     model.to("cuda:0")
@@ -294,6 +472,17 @@ def run_gate(
         find_unused_parameters=True,
         static_graph=False,
     )
+    scheduler, _ = build_scheduler(
+        copy.deepcopy(cfg.scheduler),
+        optimizer,
+        int(cfg.workflow.expected_train_batches_per_epoch),
+    )
+    scheduler.step(bridge_ready)
+    _require(
+        int(scheduler.last_epoch) == bridge_ready,
+        "scheduler could not be aligned to the detector-gradient endpoint",
+    )
+    model_ema = ModelEma(ddp)
     optimizer.zero_grad(set_to_none=True)
     try:
         with torch.autocast(
@@ -330,6 +519,16 @@ def run_gate(
         positions,
         batch["masks"],
     )
+    real_cost_update = _run_real_cost_optimizer_update(
+        ddp=ddp,
+        model=model,
+        optimizer=optimizer,
+        scheduler=scheduler,
+        model_ema=model_ema,
+        batch=batch,
+        clip_grad_norm=float(cfg.solver.clip_grad_norm),
+        max_retries=int(cfg.workflow.max_amp_retries_per_batch),
+    )
 
     rho_arm = (
         str(cfg.duca_transition_only_contract.route)
@@ -389,6 +588,12 @@ def run_gate(
             "path": str(pretrain),
             "sha256": str(adatad_pretrain_sha256).lower(),
         },
+        "official_asformer_source": {
+            "path": str(asformer_source["source_file"]),
+            "normalized_lf_sha256": str(
+                asformer_source["source_normalized_lf_sha256"]
+            ),
+        },
         "real_thumos_loader_executed": True,
         "real_model": {
             "detector": model.__class__.__name__,
@@ -412,6 +617,7 @@ def run_gate(
             "transition_auxiliary_only": transition_gradients,
         },
         "optimizer_exact_coverage": True,
+        "real_cost_optimizer_update": real_cost_update,
         "paper_claim_allowed": False,
     }
     output.parent.mkdir(parents=True, exist_ok=True)
diff --git a/tools/bata/run_duca_protected_e2e_hard_soft_gate.py b/tools/bata/run_duca_protected_e2e_hard_soft_gate.py
index aa717ad..a15a5df 100644
--- a/tools/bata/run_duca_protected_e2e_hard_soft_gate.py
+++ b/tools/bata/run_duca_protected_e2e_hard_soft_gate.py
@@ -31,6 +31,7 @@ from opentad.models.duca.hard_soft_alignment import (
     preregistered_hard_soft_gate,
     surrogate_hard_swap_descent,
 )
+from tools.bata.duca_cellcf_training import canonical_sha256
 from tools.bata.validate_duca_protected_e2e_official60 import validate_config
 
 
@@ -38,6 +39,20 @@ SCHEMA = "duca_protected_e2e_hard_soft_alignment_gate_v1"
 DEFAULT_CONFIG = (
     "configs/adatad/thumos/duca_protected_e2e_fixed384_official60.py"
 )
+EXPECTED_AUDIT_EVIDENCE_SHA256 = (
+    "1bc2a8e5062d53368ce531377599288f44d938e1e655413c755d64ac90ccdace"
+)
+EXPECTED_AUDIT_BINDINGS = {
+    "seed": 0,
+    "config_sha256": "03c59af0251cc8aa12e0b71ecb0ddc1f6f46d56de4f20dc6056f342905b8be53",
+    "resolved_config_sha256": "bc63a8453a48644cd8b175a3e0ffa346a5257b9a196e0e3dcb4a62fe208642d8",
+    "runtime_config_sha256": "624c908a0d3a6b0c958eef09643263c4b34c63ccce1dfc88d90a1514e1ff6c66",
+    "evaluation_runtime_config_sha256": "9b3ea63a5d09204580c37712e83e846cae4759523f5744d46f5e14cbd041620c",
+    "protocol_sha256": "deb1b97f00965a8fb4e0660d1c449bf58038c3e2b5ac4ea8d17375f5841b9db4",
+    "ordered_exposure_sha256": "4cba2da80fd31f7b0eb1b798c4dd907548a9f32c680cfd4b26b94d12cfcb0a35",
+    "real_loader_gate_sha256": "3d630a323e79c694f663c31151c070fd46943296937ceafdd5f9bcacfcbd7cde",
+    "ddp_pilot_sha256": "8e6a59e92f12b15ec1e7c3671104959c0533c9ba9b68dd36550c0294c8b48cd3",
+}
 
 
 class GateFailure(RuntimeError):
@@ -126,6 +141,10 @@ def _load_trained_checkpoint(
         re.fullmatch(r"[0-9a-f]{64}", str(evidence_sha256).lower()) is not None,
         "--checkpoint-evidence-sha256 must be an exact SHA256",
     )
+    _require(
+        str(evidence_sha256).lower() == EXPECTED_AUDIT_EVIDENCE_SHA256,
+        "checkpoint evidence is not the preregistered audit artifact",
+    )
     _require(
         _sha256(evidence_file) == str(evidence_sha256).lower(),
         "trained checkpoint evidence SHA256 mismatch",
@@ -163,6 +182,29 @@ def _load_trained_checkpoint(
         evidence.get("non_finite_collapse") is False,
         "checkpoint evidence reports non-finite collapse",
     )
+    unsigned_evidence = dict(evidence)
+    artifact_chain_sha256 = unsigned_evidence.pop("artifact_chain_sha256", None)
+    _require(
+        artifact_chain_sha256 == canonical_sha256(unsigned_evidence),
+        "checkpoint evidence artifact-chain hash mismatch",
+    )
+    for key, expected in EXPECTED_AUDIT_BINDINGS.items():
+        observed = evidence.get(key)
+        _require(
+            observed == expected,
+            f"checkpoint evidence binding mismatch for {key}: {observed!r}",
+        )
+    for path_key, hash_key in (
+        ("training_audit_path", "training_audit_sha256"),
+        ("checkpoint_sidecar_path", "checkpoint_sidecar_sha256"),
+        ("terminal_evaluation_path", "terminal_evaluation_sha256"),
+    ):
+        artifact_path = Path(str(evidence.get(path_key))).resolve()
+        _require(
+            artifact_path.is_file()
+            and _sha256(artifact_path) == evidence.get(hash_key),
+            f"checkpoint evidence artifact binding is invalid: {path_key}",
+        )
     manifest_path = Path(str(evidence.get("run_manifest_path"))).resolve()
     _require(
         manifest_path.is_file()
@@ -173,10 +215,19 @@ def _load_trained_checkpoint(
     _require(
         manifest.get("git_commit") == str(source_commit).lower()
         and manifest.get("variant") == "transition_beta0"
-        and manifest.get("training_profile") == "exposure132"
+        and manifest.get("schema") == "duca_cellcf_run_manifest_v1"
+        and manifest.get("task") == "offline_temporal_action_detection"
+        and manifest.get("terminal_checkpoint") == "epoch_131.pth/state_dict_ema"
+        and int(manifest.get("checkpoint_interval", -1)) == 5
         and int(manifest.get("expected_successful_optimizer_updates", -1)) == 13200,
         "checkpoint run manifest protocol mismatch",
     )
+    for key, expected in EXPECTED_AUDIT_BINDINGS.items():
+        observed = manifest.get(key)
+        _require(
+            observed == expected,
+            f"checkpoint run manifest binding mismatch for {key}: {observed!r}",
+        )
     payload = torch.load(path, map_location="cpu")
     _require(isinstance(payload, Mapping), "trained checkpoint must be a mapping")
     _require("state_dict_ema" in payload, "trained checkpoint must contain terminal state_dict_ema")
@@ -376,6 +427,11 @@ def run_gate(
         _sha256(pretrain) == str(adatad_pretrain_sha256).lower(),
         "AdaTAD pretrain SHA256 mismatch",
     )
+    _require(
+        str(adatad_pretrain_sha256).lower()
+        == str(cfg.duca_transition_only_contract.adatad_pretrain_sha256),
+        "AdaTAD pretrain hash differs from the preregistered config contract",
+    )
     _require(int(real_batches) >= 4, "pre-registered gate requires at least four real batches")
     _require(int(candidates_per_batch) >= 6, "pre-registered gate requires at least six candidates per batch")
 
@@ -398,6 +454,14 @@ def run_gate(
     model_cfg = copy.deepcopy(cfg.model)
     model_cfg.backbone.custom.pretrain = str(pretrain)
     model = build_detector(model_cfg).to("cuda:0")
+    asformer_source = (
+        model.frame_selector.raw_actionness_source.probe.official_source
+    )
+    _require(
+        asformer_source["source_normalized_lf_sha256"]
+        == cfg.duca_transition_only_contract.official_asformer_source_normalized_lf_sha256,
+        "official ASFormer source hash mismatch",
+    )
     checkpoint = _load_trained_checkpoint(
         model,
         checkpoint_path=checkpoint_path,
@@ -567,6 +631,12 @@ def run_gate(
             "path": str(pretrain),
             "sha256": str(adatad_pretrain_sha256).lower(),
         },
+        "official_asformer_source": {
+            "path": str(asformer_source["source_file"]),
+            "normalized_lf_sha256": str(
+                asformer_source["source_normalized_lf_sha256"]
+            ),
+        },
         "trained_checkpoint": checkpoint,
         "real_dataset_loader_executed": True,
         "numeric_contract": {
diff --git a/tools/bata/validate_duca_protected_e2e_official60.py b/tools/bata/validate_duca_protected_e2e_official60.py
index c0762c8..16dfaeb 100644
--- a/tools/bata/validate_duca_protected_e2e_official60.py
+++ b/tools/bata/validate_duca_protected_e2e_official60.py
@@ -26,6 +26,12 @@ OFFICIAL_CONFIG = (
     / "thumos"
     / "e2e_thumos_videomae_s_768x1_160_adapter.py"
 )
+EXPECTED_ADATAD_PRETRAIN_SHA256 = (
+    "4b96b7f403f8ae0396437855b785af6a0064f11a9d76e2268e5a76a04e0de251"
+)
+EXPECTED_ASFORMER_NORMALIZED_LF_SHA256 = (
+    "e075ee4825a201cfe324d5fbfb1332c0800f532e85b9d3809f6ca5180381c600"
+)
 
 
 def _require(condition: bool, message: str) -> None:
@@ -51,6 +57,15 @@ def validate_config(config_path: str | Path = DEFAULT_CONFIG) -> dict[str, Any]:
 
     _require(contract.task == "offline_temporal_action_detection", "task must be offline TAD")
     _require(contract.online_tad is False and contract.streaming is False, "online/streaming claims are forbidden")
+    _require(
+        contract.adatad_pretrain_sha256 == EXPECTED_ADATAD_PRETRAIN_SHA256,
+        "AdaTAD pretrain hash is not the preregistered artifact",
+    )
+    _require(
+        contract.official_asformer_source_normalized_lf_sha256
+        == EXPECTED_ASFORMER_NORMALIZED_LF_SHA256,
+        "official ASFormer source hash is not preregistered",
+    )
     _require(cfg.model.type == "ActionFormer", "detector must remain ActionFormer")
     _require(cfg.model.rpn_head.type == "ActionFormerHead", "head must remain ActionFormerHead")
     _require(_plain(cfg.model.rpn_head) == _plain(official.model.rpn_head), "official ActionFormerHead config changed")
