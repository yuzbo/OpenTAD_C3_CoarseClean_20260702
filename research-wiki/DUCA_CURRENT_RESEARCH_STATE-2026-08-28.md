# DUCA 当前研究状态（2026-08-28）

## 当前科学问题

在每个离线视频的真实 VideoMAE 高分辨率总帧预算与固定 `K=384` 完全相同时，冻结的 H65 动作性与边界语义能否把 `K=256/384/512` 预算分配给更需要它的窗口，从而优于同预算、内容无关的窗口预算置换，并保护高时间交并比定位？

## 当前唯一任务

从 clean H65 revision `04c35a3b76897e6c1569eeede41ed3aecaf7f854` 建立 `codex/duca-semantic-budget-matched-20260828`，按 Pro 修订后的合同实现真实可变 VideoMAE 分桶工作量，并在同一全视频滑窗总体上比较语义预算、内容无关匹配预算和固定 `K=384`。任务和实验合同见 `DUCA_PRO_DYNAMIC_BUDGET_DECISION-2026-08-28.md` 的“修订后的权威实验合同”。

此前发现的单窗口训练冲突已经由 Pro 解决：训练改用固定 `L=768/D=384` 的全视频滑窗，一个训练样本为一个视频并包含全部窗口；冻结 H65 侦察器只生成逐窗口语义和三种 K 的选帧表；逻辑批次为两个视频，所有 K 分桶合计只执行一次 optimizer update。候选实现已冻结为 `codex/duca-semantic-budget-matched-20260828@36d75c146492a38eb8966c66ff6b2881938cf3c6`，独立静态审查通过；N16R4 正在生成完整窗口表并执行一次真实更新的运行前检查。三个正式训练臂尚未启动。

## 当前证据

- 共享官方 AdaTAD Avg-mAP `68.73`；
- H65 30+60 Avg-mAP 约 `65.13`，是当前干净固定 K 语义非均匀选帧参考；
- 60 轮压缩和学习率日程诊断未恢复 H65；
- PJST-D1 ON−OFF Avg-mAP 点差 `-0.472481`，没有平均正向支持，但缺少配对区间；
- UVT、Fovea/Query-Bridge 和连续片段首版未恢复 H65，不进入当前候选；
- 尚无动态预算提高性能或减少真实 VideoMAE/端到端成本的正式证据。

## 当前不得声称

不得声称 DUCA 优于官方 dense、动态预算有效、动态预算已经减少实际重型计算、PJST-D1 显著为负、物理时间表示有效、或单种子结果具有稳定性。

## 下一次科学返回

完成最小实现、独立 Critic、Evaluator 运行前核验、fixed384/semantic/content-independent 三臂完整训练、官方评测、10,000 次整视频配对统计和实际成本后，将全部证据返回 Pro。只有 Pro 根据预登记门槛作出下一次继续、修订、转向或停止裁决。
