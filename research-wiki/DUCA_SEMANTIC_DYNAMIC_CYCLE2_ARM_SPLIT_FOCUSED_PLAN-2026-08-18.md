---
status: designed
evidence_class: static_contract_only
---
# Cycle 2 arm/split focused plan

Close only executable six-arm construction and FIT/CAL/HOLD entry contracts. The
real `tools/train.py` parser will accept `--duca-arm` and `--dry-run`; each arm
will construct its existing selector, while the dense placeholder remains an
explicit non-training receipt. Synthetic manifests will be validated pairwise
disjoint and deployment entries will reject GT, teacher, and raw prediction
cache fields. No detector, metric, split semantics, threshold, or checkpoint
behavior changes are in scope.
